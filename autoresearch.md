# Autoresearch: LJSpeech TTS Whisper-match quality after 10 minutes

## Objective
Optimize `examples/ljspeech.py` so that, after **10 minutes of training** on LJSpeech, generated audio for a fixed prompt is transcribed by Whisper as closely as possible to the prompt text.

We may change model architecture, losses, optimizer settings, dataset preparation, tokenization, prompt handling, generation heuristics, and training schedule. We must **not** use a pretrained TTS model. Using pretrained priors for the text LM, Mimi codec, and Whisper evaluator is allowed.

## Metrics
- **Primary**: `asr_wer_prefix` (lower is better)
- **Secondary**:
  - `asr_cer`
  - `asr_wer_in_domain`
  - `asr_wer_gt`
  - `semantic_has_eos`
  - `invalid_q0_token_count`
  - `generated_num_frames`
  - wall-clock duration

## How to Run
- Benchmark: `./autoresearch.sh`
- Because recent non-benchmark repo changes broke the benchmark environment, current runs should use:
  - `UV_NO_SYNC=1 ./autoresearch.sh ...`
- If a config or restored benchmark file surface invalidates dataset caches, prebuild with:
  - `UV_NO_SYNC=1 uv run examples/ljspeech.py --launcher dataset ...`

## Files in Scope
- `examples/ljspeech.py`
- `xax/arch/attention.py`
- `xax/arch/llm.py`
- `xax/nn/lora.py`
- `xax/task/mixins/compile.py`
- `autoresearch.sh`
- `autoresearch.md`
- `autoresearch.ideas.md`

## Off Limits
- Pretrained TTS / speech-synthesis models
- Unrelated repo files unless required to keep this benchmark runnable

## Constraints
- Keep Whisper-based prompt-match evaluation.
- Evaluate after a fixed 600-second budget.
- No pretrained TTS model.
- Prefer fast-converging improvements.
- After code edits in benchmark files, run:
  - `python -m py_compile examples/ljspeech.py xax/arch/attention.py xax/arch/llm.py xax/nn/lora.py`

## Best Known Results
- **Best recorded overall**: `0963536` at **`asr_wer_prefix=0.1667`**
  - aligned sampled exact-last spike
  - `text_source=both_weighted_sampled`
  - normalized/raw `3:1`
  - `text_source_weighted_sampled_passes=2`
  - `semantic_future_prediction_steps=4`
  - future weight `0.5`
  - scheduled sampling `0.5`
  - EOS weight `2.0`
  - p95 cap
  - grouped prefix `12`
  - `semantic_block_decode_exact_last_token=true`
  - **fragile spike, not stable baseline**
- **Historical strongest reproducible backbone on the benchmark-bearing code surface**: aligned normalized **4-head grouped-prefix** mainline at **`0.6667`**
  - tokenizer/model-vocab alignment fix active
  - `text_source=normalized`
  - `semantic_future_prediction_steps=4`
  - plain `semantic_block_decode_size=3`
  - `semantic_block_decode_max_grouped_tokens=12`
  - future weight `0.5`
  - scheduled sampling `0.5`
  - EOS weight `2.0`

## Current Resume Findings
- The older benchmark-bearing code surface around `9cea09c` is no longer cleanly represented by current HEAD (`a4e2f21` and newer unrelated commits).
- Recent resume work found a new **benchmark drift / benchmark-surface** problem:
  - `#552`: on newer HEAD/toolchain state, the formerly reproducible aligned normalized 4-head grouped backbone softened to `0.8333` with badly reduced coverage (~17.5k samples / 10 min)
  - `#553`: reverting the local compile/cache tweak invalidated caches and caused setup crashes until caches were rebuilt
  - `#555` and `#556`: restoring the older `examples/ljspeech.py` plus older Linux-enabled compile-cache behavior recovered the old throughput regime (~30k samples / 10 min) but still held at **`1.0` twice**
  - `#557-#559`: even after restoring all runtime-relevant source diffs back to the old `9cea09c` surface (`examples/ljspeech.py`, `compile.py`, `logger.py`, `supervised.py`, `asr.py`), the aligned normalized 4-head grouped backbone still only reached `1.0 -> 0.8333 -> 0.8333`
  - `#560`: explicitly pinning Qwen back to the older Hub upload snapshot `167b8104f88905a951069f5f95f9776908da5f68` did **not** help; the aligned backbone regressed to `1.0`, and the rebuilt pinned-revision train/unpadded Arrow payloads plus `model.safetensors` hash were byte-identical to the current main snapshot anyway
  - `#561`: even a **full real detached worktree** at historical commit `9cea09c` still failed cleanly at `1.0` under the current repaired environment, so the remaining "just use a dedicated old benchmark branch" hypothesis is now much weaker
  - `#562-#563`: an older `datasets==3.6.0` reconstruction exposed real cache-format / provenance drift (`Feature type 'List' not found` on current caches), and rebuilding fresh old-format caches under that older stack materially changed the dataset surface (`max_seq_len/max_audio_frames` `156/122`, filtered train size `12365`), but the aligned grouped backbone still stayed at **`1.0`** with much lower coverage (~17.4k / 10 min)
  - `#564`: a modestly older Hugging Face stack (`transformers==4.52.4`, `tokenizers 0.21.4`, `huggingface_hub 0.36.2`) also failed cleanly at **`1.0`** while preserving old-regime coverage (~30.1k / 10 min); it changed generation semantics/in-domain behavior, but still did not recover the historical `0.6667`
  - `#565`: a cleaner multi-package reconstruction attempt showed that older `datasets` cannot even read the current `tokenized/v4` cache format (`Feature type 'List' not found`), so cache-format drift extends all the way back to the tokenized stage, not just train/unpadded
  - `#566`: the first **successful comparability recovery** came from a broader old-environment reconstruction: detached `9cea09c` worktree + `datasets==3.6.0` + `transformers==4.52.4` / `tokenizers 0.21.4` / `huggingface_hub 0.36.2`, plus a fresh old-format tokenized/unpadded/train rebuild in an isolated temp data dir. Under that combined surface, the canonical aligned normalized 4-head grouped backbone finally recovered **`asr_wer_prefix=0.6667`** again with CER `0.5`, EOS on both prompts, and near-old-regime coverage (~29.5k / 10 min)
  - `#567-#569`: that apparent recovery did **not** reproduce. Two exact reruns of the same combined reconstructed environment and a brand-new full fresh rebuild into another isolated temp data dir all fell back to **`1.0`** with the same lower-coverage regime (~17.6k / 10 min). So the combined old-environment path looked like a one-off recovery clue, but not yet a stable recovered benchmark surface
  - `#570`: a broader lower-level stack probe (`jax==0.9.2`, `jaxlib==0.9.2`, `jax-cuda12-pjrt==0.9.2`, `jax-cuda12-plugin==0.9.2`, `orbax-checkpoint==0.11.33`, `optax==0.2.7`) did bring up both GPUs cleanly, but the historical benchmark code path crashed during the first train step with a new sharding/vmap incompatibility (`ValueError: ... inconsistent axis specs: batch vs None`). So a broader JAX/CUDA stack definitely changes runtime behavior, but this exact 0.9.2 stack is too new / not source-compatible for faithful drift isolation
  - `#571-#572`: a key import-surface bug was finally exposed in the detached-worktree path. Running `uv run examples/ljspeech.py` from `/tmp/xax-bench-9cea09c` is **not** automatically a fully local old-repo execution surface, because the script-style launch can still leave some `xax/...` imports resolving outside the worktree unless the worktree root itself is explicitly added to `PYTHONPATH`. After forcing `/tmp/xax-bench-9cea09c` onto `PYTHONPATH` while keeping the combined old HF/datasets environment and old-format caches, the benchmark surface changed materially: the first fully local probe restored old-regime throughput (~30.1k / 10 min) and short EOS-rich generations but still landed at `1.0`; the exact rerun then recovered **`asr_wer_prefix=0.6667`** again. So the strongest live path became the **fully local detached `9cea09c` worktree + older package stack + old-format caches**, and prior claims that the detached worktree was dead were too strong because some earlier probes were still hybrid surfaces
  - `#573`: the third fully local probe collapsed back to `1.0`, but still held the old-regime throughput regime (~29.9k / 10 min). So the import-surface fix clearly matters, yet the recovered quality is still noisy (`1.0 -> 0.6667 -> 1.0`)
  - `#574-#575`: factor isolation then showed the **old `datasets` runtime package itself is probably not required**. With the fully local detached worktree on `PYTHONPATH`, the old HF stack still overlaid, current `datasets 4.4.2`, and the old-format caches reused from `/tmp/xax-data-ds36hf452`, the canonical probe hit **`0.6667` twice** with old-regime throughput (~30k / 10 min)
  - `#576`: dropping the old HF overlay too (keeping only the detached worktree root on `PYTHONPATH` plus old-format caches) preserved the old-regime throughput but quality fell back to **`1.0`**. So the live dependency picture narrowed to **import-surface fidelity + older HF stack + old-format cache provenance**
  - `#577`: keeping the fully local detached worktree and old HF stack but pointing at the main **current-format** cache surface in `/home/ben/Experiments/Data` (`tokenized/v4`, `unpadded/d7b4569f5462acf7_e4146c08`, `train/a009c9c8eee13c4a_e4146c08`) cleanly fell back to **`1.0`** despite preserving old-regime throughput. So old-format cache provenance is genuinely live
  - `#578`: seeding a fresh temp data dir with only the **current-format tokenized/v4** and regenerating `unpadded`/`train` there under the fully local detached worktree + old HF stack also stayed at **`1.0`** with the current-style dataset surface (`max_seq_len/max_audio_frames=155/121`, filtered train size `12315`). So the tokenized-stage provenance is already enough to break recovery
  - `#579`: seeding a fresh temp data dir with only the **old-format tokenized/v4** and regenerating `unpadded`/`train` there under current `datasets 4.4.2` kept the old-style dataset surface (`156/122`, `12365`) but still fell back to **`1.0`**. So old tokenized layout alone is **not sufficient**; the surviving recovery depends on downstream old-format cache content too, not just matching those headline dataset stats
  - `#580-#582` and `#585`: the strongest new factor-isolation clue came from collapsing the cache surface all the way down to the **old train cache alone**. Providing only the old `/tmp/xax-data-ds36hf452/.../train/a009c9c8eee13c4a_e4146c08` directory (with no tokenized or unpadded caches present) under the fully local detached worktree + old HF stack yielded a very noisy but much stronger surface: `0.8333 -> 0.3333 -> 1.0 -> 1.0`
  - `#655`: a fresh revisit of that same **old-train-cache-only** route brought it back up to **`0.8333`** with old-regime throughput and clean gt-ASR. So the runtime dependence still looks genuinely alive on this narrow surface, but it remains unstable rather than recovered: current sequence is **`0.8333 -> 0.3333 -> 1.0 -> 1.0 -> 0.8333`**
  - `#583`: dropping the old HF overlay on that train-cache-only surface fell cleanly back to **`1.0`** with badly degraded gt-ASR sanity. So even on the narrowed train-only path, the older HF tokenizer/model-loading stack still looks required
  - `#584`: keeping the old HF overlay but going back to the **hybrid** import surface (not forcing the detached worktree root onto `PYTHONPATH`) produced only `0.8333` with the slow ~18k-samples/10min regime. So the local-worktree import fix still matters materially even on the train-cache-only path
  - `#586-#588`: transferring the recovered cache clues back onto the **current repo code surface** showed that current HEAD remains a real blocker even when force-fed aliased old caches. Running current HEAD with only the old train payload aliased into its expected hash name crashed first on a hash mismatch, then held at `1.0` after aliasing; running current HEAD with the full aliased old tokenized/unpadded/train surface also still held at `1.0` with the slow ~17.6k/10min regime. So the detached `9cea09c` code/import surface is still genuinely required for the stronger recovered behavior
  - `#589` and `#592`: running the **current** benchmark script while putting the detached worktree root on `PYTHONPATH` and using the old HF stack plus aliased full old caches produced **`0.6667`** and then **`0.5`** with old-regime throughput (~29.7k-29.9k / 10 min). So that mixed surface is definitely alive
  - `#593`: however, copying only the old `xax/` package tree into `/tmp/xaxpkg-oldexact` and running the same current main script with the same old HF + aliased full old caches collapsed back to **`1.0`**. So the old-root effect is **not** explained by the `xax/` package directory alone
  - `#594`: the first attempt to run a truly corrected current-script + old-`xax` surface from outside the repo root crashed immediately because `uv run` outside the project lost the benchmark environment (`ModuleNotFoundError: No module named 'equinox'`)
  - `#595`: the corrected current-script + old-`xax` probe then succeeded by launching a `/tmp/current-ljspeech-link.py` symlink to the current script through the repo’s project environment, which really did import `xax/...` from the detached worktree while keeping the current top-level script contents. That surface still failed cleanly at **`1.0`** with old-regime throughput, so the earlier mixed-surface gains were **not** simply “current script + old `xax` works fine”
  - `#596`: exposing only the **old `examples.ljspeech` namespace resolution** (via `/tmp/oldexamplesonly/examples/ljspeech.py`) while keeping current `xax/...` also failed cleanly at **`1.0`** with the slow ~18.1k/10min regime. So old `examples` resolution alone is not enough either
  - `#597-#598`: a synthetic hybrid with **old `xax/...` resolution** (`/tmp/xaxpkg-oldexact`) **plus old `examples.ljspeech` namespace resolution** (`/tmp/oldexamplesonly/examples/ljspeech.py`) recovered **`0.6667`** once and then weakened to **`0.8333`**, both with old-regime throughput. So that split old-overlays surface is real, but noisy
  - `#599-#600`: the exact current main-script path is **not** required. Running either a symlinked or hard-copied current script through the same old-HF + aliased-old-cache mixed surface still landed in the live regime (**`0.5`** and **`0.8333`** respectively). So this is not simple file-path identity
  - `#601`: the current repo root on `PYTHONPATH` is also **not** required. The hard-copied current script with only the split old overlays (`/tmp/xaxpkg-oldexact:/tmp/oldexamplesonly`) plus old HF and aliased full old caches still gave **`0.8333`**
  - `#602`: but the **symlinked** current script on that same no-repo-root surface fell cleanly back to **`1.0`**
  - `#603`: replacing the split overlays with the **unified detached-worktree root** (`/tmp/xax-bench-9cea09c`) while keeping the same hard-copied current script still held **`0.8333`**. So the corrected-symlink failure was **not** about needing split overlays instead of the unified old root
  - `#604`: a hard copy named `/tmp/examples.ljspeech.py` — which still falls back to `task_module=examples.ljspeech` under `_path_to_module_name(...)` — also held **`0.8333`**. So `task_module=examples.ljspeech` by itself is **not** enough to explain the bad symlinked surface
  - `#605`: an operator-error crash while trying to rerun the bad symlinked surface still let the underlying training job finish; it again landed at **`1.0`**, which weakly supports the original bad-symlink clue
  - `#606`: symlinking to the copied script itself (`/tmp/current-copy-link.py -> /tmp/current_ljspeech_copy.py`) also failed at **`1.0`**. So the copied-script gain is **not** just about the resolved target path or fallback module name; the symlink itself appears to matter materially
  - `#607`: the strongest new clue came from disabling spawned monitor processes. On the failing symlink-to-copy surface, adding `disable_multiprocessing=true` rescued the run back to **`0.6667`** with old-regime throughput
  - `#608`: the same `disable_multiprocessing=true` probe on the hard-copied current-script surface improved it from `0.8333` to **`0.5`**. So spawned multiprocessing is not just a symlink-only nuisance; it is now a genuinely live benchmark-surface lever more broadly
  - `#609`: the exact rerun of the symlink-to-copy + `disable_multiprocessing=true` rescue collapsed back to **`1.0`**. So the multiprocessing clue is real, but still noisy on the symlinked surface
  - `#610`: switching the bad symlink-to-copy surface from the default multiprocessing context to **`multiprocessing_context=fork`** improved it from `1.0` to **`0.8333`**. So the problem is not merely “any multiprocessing”; the default **spawn-style** subprocess surface looks materially worse than `fork`
  - `#611`: the matching copied-current-script `multiprocessing_context=fork` probe stayed at **`0.8333`**, not better than the copied-script baseline. So `fork` mainly helps the bad symlinked spawn surface; the copied-script surface benefits more from removing the manager/process machinery entirely than from simply changing the start method
  - `#612-#613`: patching the old `xax` overlay to disable only the **CPU stats** subprocess or only the **GPU stats** subprocess, one at a time, did **not** rescue the bad symlink-to-copy surface; both still held at **`1.0`**. So the `disable_multiprocessing=true` rescue is not explained by either monitor in isolation
  - `#614`: even disabling **both** monitor subprocesses while still leaving the broader `ProcessMixin` multiprocessing context / manager surface intact also held at **`1.0`**. So the culprit is not the stats workers themselves, either individually or together
  - `#615-#616`: patching the old `xax` overlay so `ProcessMixin` still creates a multiprocessing **context** but does **not** create the **`SyncManager`** recovered **`0.6667`** twice on the bad symlink-to-copy surface with old-regime throughput. That made the live repo-surface culprit look much more specifically like the **`SyncManager` creation** inside `ProcessMixin`
  - `#617`: the matching no-`SyncManager` probe on the **hard-copied current-script** surface did **not** help; it fell cleanly to **`1.0`**. So the `SyncManager` bug looks especially tied to the **symlinked entrypoint** path rather than being a universal penalty across all current-script-copy surfaces
  - `#618`: the **third** exact rerun of the no-`SyncManager` symlinked surface weakened to **`0.8333`**. So the no-manager rescue is clearly real relative to the raw bad-symlink `1.0` class, but it is **not perfectly stable**; current picture is `0.6667 -> 0.6667 -> 0.8333`
  - `#619-#620`: a sharper factor-isolation test used the old overlay with **both CPU and GPU stats monitors disabled**, leaving only the broader `ProcessMixin` manager/process machinery active, and then switched to **`multiprocessing_context=fork`**. Unlike the spawn manager-only probe `#614` that stayed at `1.0`, the fork-manager-only surface gave **`0.6667`** and then **`0.8333`** with old-regime throughput. So the bad surface is not about manager existence alone, but especially about the **spawned `SyncManager` process** on the symlinked entrypoint path
  - `#621`: creating the manager with **`fork`** while leaving the rest of the multiprocessing surface unchanged — i.e. the stats workers still use the default **spawn** context — did **not** help; the bad symlinked surface stayed at **`1.0`**. So a forked manager alone is **not sufficient** once the spawned workers are back in play
  - `#622-#623`: worker isolation on top of that fork-manager surface then showed the remaining spawned-worker damage is **asymmetric**:
    - with only the **CPU stats worker** left active under spawn, the surface still failed cleanly at **`1.0`**
    - with only the **GPU stats worker** left active under spawn, it weakened only to **`0.8333`**
    So after moving the manager off spawn, the **CPU monitor worker** now looks like the harsher remaining bad actor, while the GPU worker alone is only weakly harmful
  - `#624`: forcing that remaining **CPU monitor worker** itself to use **`fork`** (with forked manager and GPU monitor disabled) improved the CPU-only case from `1.0` to **`0.8333`**. So the CPU worker's damage is materially tied to its **spawned bootstrap**, not just to CPU monitoring logic in the abstract
  - `#625-#626`: moving **all three children** off spawn — manager, CPU monitor, and GPU monitor all forced to **`fork`** — recovered **`0.6667`** once and then collapsed to **`1.0`**. So the all-forked repair is the clearest mechanistic rescue so far, but it is still noisy rather than a stable restored benchmark surface
  - `#627`: lightweight bootstrap instrumentation on the **spawned CPU stats worker** (with forked manager + GPU disabled) dumped a key direct clue: the spawned CPU child starts with `main_file=/tmp/current-copy-link.py`, `argv0=/tmp/current-copy-link.py`, and `sys.path` beginning with `/tmp`, then the old overlay roots. In other words, that spawned CPU worker is literally bootstrapping through the **symlinked entry script** as `__main__`
  - `#629`: the matching direct bootstrap instrumentation on the **spawned manager child** showed the same thing on the bad symlinked surface: `main_file=/tmp/current-copy-link.py`, `argv0=/tmp/current-copy-link.py`, and `sys.path` again beginning with `/tmp` before the old overlay roots. So one of the main bad spawned-child paths now directly proves it is re-entering through the **symlinked** main-module surface
  - `#628`: running that same manager-child instrumentation on the **hard-copied** current-script surface showed the expected contrast: the spawned manager child starts instead with `main_file=/tmp/current_ljspeech_copy.py` and `argv0=/tmp/current_ljspeech_copy.py`, while `sys.path` still begins with `/tmp`. So the entrypoint-form difference really does propagate directly into spawned-child bootstrap state
  - `#630`: the same copied-vs-symlinked comparison is now established for the **CPU stats worker** too. On the hard-copied surface, the spawned CPU child starts with `main_file=/tmp/current_ljspeech_copy.py` and `argv0=/tmp/current_ljspeech_copy.py`, mirroring the manager-child behavior. So **both** spawned-child types we care about now show the same exact split: on the bad surface they re-enter through the **symlinked** main script, and on the copied surface they re-enter through the hard copy instead
  - `#631`: the converse sufficiency test gave the strongest causal evidence yet. Starting from the normally stronger **hard-copied** surface, forcing a temp copied script to present the **symlink path** `/tmp/current-copy-link.py` as `__main__.__file__` / `sys.argv[0]` collapsed it cleanly to **`1.0`**. So presenting the symlinked main-path identity is enough to poison an otherwise better copied surface
  - `#632`: but the opposite direction is **not** symmetric. Launching a symlinked surface whose startup prelude rewrites `__main__.__file__` / `sys.argv[0]` to the resolved realpath still failed at **`1.0`**. So changing those fields from *inside* the already-symlink-launched process is **not sufficient** to rescue the surface once bootstrap has already happened
  - `#633`: separating that forced-symlink-main poison from the `ProcessMixin` child layer showed a real split. Keeping the copied script that force-presents `/tmp/current-copy-link.py` as `__main__`, but adding `disable_multiprocessing=true`, improved the surface from `1.0` to **`0.8333`** with old-regime throughput and EOS on both prompts. So a large part of the full poison clearly routes through the `SyncManager` / monitor subprocess layer, even though the symlinked main-path identity is still harmful without it
  - `#634`: however, simply changing that same forced-symlink-main copied surface to `multiprocessing_context=fork` did **not** rescue it; it stayed at **`1.0`**. So the copied-surface poison is not reducible to “spawn bad, fork good” once the manager/monitor machinery remains enabled
  - cheap off-benchmark code-object probe: actual **symlink launch** vs **forced-symlink-main copy** now have a concrete parent-side difference that survives any in-script rewrite. In a minimal spawn repro, forcing `__main__.__file__` / `argv0` to the symlink path changed parent `main_file` and even `inspect.getfile(...)`, but the parent still kept its original loader/code provenance (`__loader__.path` and `Dummy.marker.__code__.co_filename` stayed on the copy path). In the spawned child, by contrast, the forced case *did* come up with the symlink path as `co_filename`, matching the real symlink launch. So there is now direct evidence that some **parent-side compiled/loader path state is fixed before the first line of the script runs**, which explains why in-process canonicalization is too late to be a full repair
  - `#635`: a stronger wrapper-based repair then directly tested that theory and failed. A symlinked wrapper rebound `__main__` to `/tmp/current_ljspeech_copy.py`, installed a real `SourceFileLoader`, set `sys.argv[0]` to the copy path, and `exec`-compiled the copied benchmark script so both parent and spawned-child code/object provenance looked like a real copy launch in a cheap probe. The benchmark logs also reported `/tmp/current_ljspeech_copy.py` as the task path / command line. Even so, the surface still failed cleanly at **`1.0`**. So merely making the benchmark code *look* like a real copy launch from inside a wrapper is not enough
  - `#636`: the immediate clean control rerun of the plain **hard-copied** current-script surface under the same split-old-overlays / old-HF / aliased-old-cache setup still hit **`0.8333`** with old-regime throughput. That makes `#635` much more informative: the wrapper itself (or some outer bootstrap state it leaves behind) is a real remaining difference from a true copied-script launch, not just ordinary run-to-run noise
  - `#637`: rerunning that same exec-real wrapper **directly** rather than through a symlink still failed at **`1.0`**. So the wrapper's failure is **not** specifically about the wrapper file being symlinked; the outer exec-wrapper launch surface itself is materially worse than a true copied-script launch
  - `#638`: but that wrapper surface is still strongly coupled to the `ProcessMixin` child layer. Keeping the same direct exec-real wrapper, but adding `disable_multiprocessing=true`, improved it sharply to **`0.5`** with old-regime throughput, EOS on both prompts, and clean gt-ASR. So even after canonicalizing the benchmark code itself to the copy path, the remaining wrapper harm still routes heavily through the `SyncManager` / monitor-process surface rather than living purely in the parent process
  - `#639`: removing **only** the `SyncManager` from that same direct exec-real wrapper surface did **not** reproduce the `disable_multiprocessing=true` rescue; it stayed at **`1.0`**. So on the wrapper surface, the remaining harm is broader than manager creation alone and appears to require the wider monitoring/process layer
  - `#640`: disabling **both** CPU and GPU monitor subprocesses on that same direct exec-real wrapper surface rescued it to **`0.6667`** with old-regime throughput and perfect in-domain prompt match. So the strongest remaining wrapper harm is now clearly inside the **stats-monitor subprocess layer**, not just in the parent wrapper/bootstrap surface
  - `#641-#643`: the wrapper-side monitor story is now asymmetric and sharper:
    - **GPU monitor only** (CPU disabled) still fails cleanly at **`1.0`**
    - **CPU monitor only** (GPU disabled) improves to only **`0.8333`**
    - **GPU monitor only + forked manager** still fails at **`1.0`**
    So on the exec-real wrapper surface, the **GPU monitor path** is the harsher remaining culprit, and its damage is **not** explained away by the manager being spawned
  - `#644`: the key wrapper-side rescue then landed exactly where the notes were pointing. Keeping the **GPU monitor as the only active monitor subprocess** (CPU disabled), but forcing that **GPU worker itself off spawn** while also using the forked-manager surface, rescued the wrapper from `1.0` down to **`0.6667`** with old-regime throughput, EOS on both prompts, clean gt-ASR, and much better in-domain prompt match. So the wrapper-side GPU-monitor damage is now directly shown to be materially tied to the **GPU worker's spawned bootstrap itself**
  - `#645`: factor-isolating that rescue showed the GPU-worker change matters on its own, but is not the whole story. Keeping the default **spawned manager** while forcing only the **GPU worker** off spawn improved the wrapper from `1.0` to only **`0.8333`**. So the strongest wrapper-side recovery still appears to need **both** a non-spawn manager and a non-spawn GPU worker
  - `#646`: the matching CPU-side symmetry check was much weaker. On the wrapper surface with **GPU disabled**, forcing the **CPU worker** off spawn (and using a forked manager) still held only **`0.8333`**, essentially no better than the earlier CPU-only wrapper case. So unlike the GPU path, the wrapper-side **CPU monitor** damage does **not** look strongly tied to its spawn bootstrap
  - `#647`: direct **wrapper-side GPU bootstrap instrumentation** then added a sharp explanatory split. On the wrapper **GPU-only** spawn surface, the metric itself was noisy and tied **`0.6667`**, but the spawned **GPU worker** dump showed **`argv0=/tmp/current_ljspeech_copy.py`**, **`main_file=/tmp/current_ljspeech_copy.py`**, `main_name=__mp_main__`, `main_spec=null`, and `sys.path[0]=/tmp`. So unlike the earlier bad symlinked CPU/manager children, the wrapper-side spawned GPU worker already sees the **copied benchmark path** as `__main__`
  - `#648`: the complementary **wrapper-side manager-child** probe showed the same thing. On the no-stats wrapper surface, the spawned **manager child** also came up with **`argv0=/tmp/current_ljspeech_copy.py`**, **`main_file=/tmp/current_ljspeech_copy.py`**, `main_name=__mp_main__`, and `sys.path[0]=/tmp`, while the metric again tied **`0.6667`**. So on the exec-real wrapper surface, **both** child types directly inspected there now already see the copied benchmark path as `__main__`
  - `#649`: the first **true copied-script control** for that new wrapper-side GPU dump then matched it almost exactly. On the direct copied-script surface with the same GPU-only debug overlay, the spawned **GPU worker** again came up with **`argv0=/tmp/current_ljspeech_copy.py`**, **`main_file=/tmp/current_ljspeech_copy.py`**, `main_name=__mp_main__`, `main_spec=null`, `main_loader=null`, and the same `sys.path` prefix, while the metric landed at **`0.8333`**. So the wrapper-vs-copy split is **not** explained by any obvious visible GPU-worker `__main__` / loader / spec payload difference
  - `#650`: the matching **true copied-script control** for the manager-child dump also matched the wrapper probe. On the direct copied-script no-stats surface, the spawned **manager child** again came up with **`argv0=/tmp/current_ljspeech_copy.py`**, **`main_file=/tmp/current_ljspeech_copy.py`**, `main_name=__mp_main__`, `main_spec=null`, and the same `sys.path` prefix, while the metric again landed at **`0.8333`**. So the wrapper-vs-copy gap is now even less consistent with a visible child-bootstrap `__main__` difference and more concentrated in some **parent/bootstrap or hidden spawn-preparation state**
  - `#651`: a new `sitecustomize.py` probe then dumped **`multiprocessing.spawn.get_preparation_data(...)`** in the parent plus **`multiprocessing.spawn.prepare(...)`** in the child on the **wrapper GPU-only** surface. Quality was poor (`1.0`), but the key dump is that for both **`SyncManager-1`** and **`xax-gpu-stats`**, the wrapper-side prep payload already used **`init_main_from_path=/tmp/current_ljspeech_copy.py`**, `sys_argv[0]=/tmp/current_ljspeech_copy.py`, the expected overlay `sys_path`, and even reported visible `start_method='fork'`; the child `prepare(...)` dumps echoed those same fields
  - `#652`: the matching **true copied-script control** for that spawn-prep probe then showed the same thing. On the direct copied-script GPU-only surface, the parent `get_preparation_data(...)` dumps and child `prepare(...)` dumps for both **`SyncManager-1`** and **`xax-gpu-stats`** were **effectively identical** to the wrapper probe apart from run-specific values like authkey and run dir. So the wrapper-vs-copy gap is now **not** explained by any obvious visible `multiprocessing.spawn` prep-data difference either
  - `#653`: a still-deeper parent-side launch probe then patched **`popen_spawn_posix.Popen._launch`**, **`spawn.get_command_line`**, **`util.spawnv_passfds`**, and **`reduction.dump`** on the wrapper GPU-only surface. That run unexpectedly improved to **`0.5`**, but the more important result is the new launch metadata: the spawned **manager** used a normal `SpawnProcess` launch to `multiprocessing.managers.BaseManager._run_server`, the usual `spawn_main(...)` command line under the project Python, `passfds=[8,6,10,9]`, and a process-object pickle size of **`1891` bytes**; the wrapper-side **GPU worker** launch was captured similarly
  - `#654`: the matching **true copied-script control** for that deeper launch probe also hit **`0.5`**, and the copied-script launch metadata matched the wrapper probe at this deeper visible parent-launch level too — same manager launch shape, same `spawn_main(...)` command line, same manager process-object pickle size (**`1891` bytes**), and the same GPU-worker launch pattern / passfds shape up to run-specific values. So the wrapper-vs-copy gap is now **not** explained by any obvious visible child payloads, visible `multiprocessing.spawn` prep data, or this deeper visible `Popen._launch` / `spawnv_passfds` metadata either
  - `#656-#657`: a deeper **manager-child inspect** overlay on the bad **symlink-to-copy** spawn-manager-only surface then sharpened the symlink explanation further. The symlinked run unexpectedly improved all the way to **`0.3333`**, while the matching **true copied-script control** on the same old-HF + aliased-old-cache split-overlay surface fell back to **`1.0`**, both with the same old-regime throughput. The paired manager-child dumps matched on the visible module-resolution surfaces that were still live hypotheses — `_path_to_module_name(Path(main_file))` already resolved to **`current_ljspeech_copy`** in **both** cases, `examples.ljspeech` was absent from `sys.modules` in **both** cases, `sys.path` matched, and the child was still just `__main__/__mp_main__` in both cases. The materially different visible fields were now even narrower: the raw **`main_file` / `inspect.getfile(__main__)`** path itself (`/tmp/current-copy-link.py` vs `/tmp/current_ljspeech_copy.py`) and the **`co_filename`** of main-defined functions such as `weighted_text_sample_bucket`, which changed in lockstep with that raw path. So the bad-symlink story is now even less consistent with a simple module-name-resolution difference and more concentrated in the **raw main-module / code-object filename identity** itself
  - `#658`: the simplest immediate ablation of that new clue already failed. A minimal manager-child overlay that did little more than touch **`weighted_text_sample_bucket.__code__.co_filename`** on the bad symlinked spawn-manager-only surface fell straight back to **`1.0`** while still showing the expected symlinked code-file identity. So the `#656` improvement is **not** explained by merely touching one main-defined function's code filename in the manager child; that simple rescue story is now pruned
  - `#659`: an exact rerun of the full **manager-child inspect** overlay from `#656` also collapsed straight back to **`1.0`** on the same bad symlinked spawn-manager-only surface. So the headline `0.3333` from `#656` is **not reproducible as-is**; the full manager-inspect overlay is now downgraded from “promising repair” to a likely **noise spike or fragile broad side effect**
  - `#660-#661`: two new factor-isolation probes on the fully local detached-worktree **old-train-cache-only** route then tested whether its bimodality might also be driven by the `ProcessMixin` child layer. They did **not** support that: adding **`disable_multiprocessing=true`** stayed at **`0.8333`**, and changing only **`multiprocessing_context=fork`** fell to **`1.0`**. So unlike the mixed current-script surfaces, the detached old-train-only route does **not** currently look strongly rescuable by simple multiprocessing controls
  - `#662-#665`: a new **symlink-surface code-object canonicalization** probe then targeted the remaining bad-symlink hypothesis more directly. I built a temp script (`/tmp/current_ljspeech_copy_canoncode.py`, launched via `/tmp/current-canoncode-link.py`) that keeps the **symlinked outer launch surface**, but rewrites `__main__.__file__` / `argv0` to **`/tmp/current_ljspeech_copy.py`** and, just before launch, rewrites top-level function / method **`co_filename`** values to that same copied-script path. The first run crashed only because the modified script changed the dataset-function hash and invalidated caches; after prebuilding its new `unpadded/train` hashes, the benchmark improved once to **`0.8333`** on the bad symlinked **spawn-manager-only** surface with old-regime throughput. But the follow-up controls then collapsed the branch: launching the **same temp script directly** on the **same rebuilt cache surface** fell back to **`1.0`**, and the exact rerun of the **symlinked** temp-script surface also fell back to **`1.0`**. So the whole temp-script canonicalization path is now best treated as another **noise-prone side clue**, not a stable repair or clean proof about raw `co_filename` consumers
  - `#666-#694`: the next control batch sharpened the **monitor-layer** picture on both the mixed current-script surfaces **and** the detached historical old-train-only route. Two exact reruns first pruned the broader **`disable_multiprocessing=true`** rescue class on mixed current-script surfaces: the earlier **hard-copied current-script** `0.5` collapsed back to **`1.0`** (`#666`), and the earlier **direct exec-real wrapper** `0.5` also collapsed back to **`1.0`** (`#667`). But the narrower **no-stats** intervention stayed meaningfully alive: the exact rerun of the **wrapper + no-stats** surface weakened from `0.6667` to **`0.8333`** (`#668`), and the missing **hard-copied current-script + no-stats** control landed at **`0.6667`** before weakening to **`0.8333`** on exact rerun (`#669-#670`). A copied-surface **GPU-only** control then showed that simply putting the **GPU monitor** back is already enough to knock that copied no-stats surface down to **`0.8333`** (`#671`), though still not all the way to the wrapper GPU-only `1.0` class; and the complementary copied-surface **CPU-only** control improved sharply to **`0.5`** before collapsing straight back to **`1.0`** on exact rerun (`#672-#673`). The next copied-surface GPU isolations then sharpened the mechanism further: adding a **forked manager** alone to copied **GPU-only** stayed at **`0.8333`** (`#674`), moving **both** the manager and GPU worker off spawn improved once to **`0.6667`** before collapsing back to **`1.0`** on exact rerun (`#675-#676`), and forcing the **GPU worker off spawn alone** under the default spawned manager failed cleanly at **`1.0`** (`#677`). Two bridge tests then pruned “clean up the copied no-stats surface and transfer it”: replacing the split overlays with a unified old-root no-stats overlay failed at **`1.0`** (`#678`), and moving the copied no-stats surface onto the narrower **old-train-only** cache route also failed at **`1.0`** (`#679`). But the detached historical route then became newly alive again: building a fully local **old-train-only + no-stats** overlay (`/tmp/xax-bench-9cea09c-oldtrain-nostats`) improved the canonical aligned normalized 4-head grouped probe to **`0.6667 -> 0.5 -> 0.5`** across `#680-#682`, before weakening back to **`0.8333`** on the fourth exact probe (`#693`). Splitting that detached rescue showed a different asymmetry from the copied/wrapper surfaces: reintroducing only the **GPU monitor** weakened it to **`0.6667 -> 0.8333`** (`#683`, `#685`), while reintroducing only the **CPU monitor** collapsed it cleanly to **`1.0`** (`#684`). The next CPU-side factor isolation then sharpened that detached route further: forcing the **CPU worker itself** off spawn on the CPU-only branch improved it reproducibly to **`0.8333 -> 0.8333`** (`#686-#687`), while forcing only the **manager** off spawn with the CPU worker still spawned did **not** help (`#688`). By contrast, forcing the detached **GPU worker** off spawn on the GPU-only branch also did **not** help (`#689`). The missing completion of that detached CPU split then landed much stronger than expected: moving **both** the **manager** and the **CPU worker** off spawn on the CPU-only branch improved sharply to **`0.3333`** (`#690`) with near-old-regime throughput and clean gt-ASR, but the exact rerun weakened back to **`0.8333`** (`#691`). A parallel manager test on the broader **no-stats** branch then failed cleanly at **`1.0`** (`#692`), showing that the no-stats route does **not** want a forked manager even though the CPU-only branch sometimes does. Finally, moving **all three child paths** off spawn — manager, CPU worker, and GPU worker all forced to `fork` — improved the detached old-train-only route to **`0.6667 -> 0.6667 -> 1.0`** across `#694-#696`, so even that broader all-fork rescue is not stable. The next matrix-filling probes then sharpened the full-route story further: forcing only the **CPU worker** off spawn still failed at **`1.0`** (`#697`), forcing **manager + CPU** off spawn improved once to **`0.6667`** then fell back to **`1.0`** (`#698-#699`), slowing the raw **CPU-only** monitor cadence to `cpu_stats.ping_interval=10` still failed at **`1.0`** (`#700`), forcing only the **manager** off spawn on the full route also failed at **`1.0`** (`#701`), forcing **both workers** off spawn with the manager left on its default surface improved once to **`0.5`** then fell back to **`1.0`** (`#702-#703`), forcing **manager + GPU** off spawn while leaving only the **CPU worker** spawned still failed at **`1.0`** (`#704`), and the missing **GPU-only** full-route corner then improved weakly to **`0.8333`** (`#705`). Two follow-up timing probes then pruned a narrower rescue story on the detached **CPU-only** route too: delaying the spawned CPU worker until **120s** into training still failed at **`1.0`** (`#706`), and delaying it until **~540s** — almost the very end of the run — also still failed at **`1.0`** (`#707`). The next transport/control probes then pruned two more simplified CPU-only stories: keeping the spawned CPU worker but replacing its **manager-backed proxy transport** with direct `ctx.Event()` / `ctx.Value(...)` still failed at **`1.0`** (`#708`), and moving the same CPU-monitor logic fully **inline on the training process** also still failed at **`1.0`** while dragging throughput down to only ~26.6k samples / 10 min (`#709`). The next detached CPU-only split then sharpened the mechanism further in seventeen useful directions: keeping the default spawned CPU worker but reducing it to a **dummy sleeping child** with no `psutil` work and no stats emission improved cleanly to **`0.6667`** (`#710`), keeping that same spawned worker but reducing it only to **parent-only `psutil` sampling** still failed at **`1.0`** (`#711`), keeping the spawned worker plus **zero-valued stats emission** with no `psutil` at all improved only to **`0.8333`** (`#712`), reducing the worker further to **`proc.cpu_percent()` alone** still improved only to **`0.8333`** (`#713`), reducing it to **`proc.memory_info()` alone** then improved cleanly back to **`0.6667`** (`#714`), reducing it to **`proc.memory_percent()` alone** also improved only to **`0.8333`** (`#715`), keeping only the two-percent combo **`cpu_percent() + memory_percent()`** still improved only to **`0.8333`** (`#716`), keeping the mixed memory pair **`memory_info() + memory_percent()`** also recovered the stronger **`0.6667`** class (`#717`), and then testing the last unexplained pair **`cpu_percent() + memory_info()`** improved further to **`0.5`**, weakened to **`0.6667`** on exact rerun, and then collapsed back to **`1.0`** on the third probe (`#718-#720`). A narrow **warmup** follow-up then made that pair look less like a stable baseline and more like a call-level quirk: adding a one-call **`cpu_percent()` warmup** before the same `memory_info() -> cpu_percent()` loop weakened the branch only to **`0.8333`** (`#721`). The next two sequencing probes then made the surviving clue much sharper: simply reversing the per-interval order to **`cpu_percent() -> memory_info()`** collapsed the branch cleanly to **`1.0`** (`#722`), and adding the same one-call `cpu_percent()` warmup on top of that reversed order still held it at **`1.0`** (`#723`). The next sequencing/value-consumption probe then narrowed it one notch further: merely **touching** `cpu_percent()` before `memory_info()` is **not** enough by itself to keep the bad class, because `cpu_percent() -> memory_info() -> cpu_percent()` only weakened to **`0.8333`** (`#724`). The next twelve follow-ups then made the picture sharper still: doing **`memory_info()` first**, then touching `cpu_percent()`, then logging a **second** `cpu_percent()` still recovered **`0.6667`** (`#725`), while touching `cpu_percent()` first and then keeping **memory-info-only** stats weakened to **`0.8333`** (`#726`). The next two two-read splits both collapsed cleanly to **`1.0`** when the emitted value was the **first** `cpu_percent()` reading for that interval: first on the **`cpu_percent() -> memory_info() -> cpu_percent()`** surface when the logged value stayed the **pre-`memory_info()`** read (`#727`), and then even on the **`memory_info() -> cpu_percent() -> cpu_percent()`** surface when the logged value stayed the **first post-`memory_info()`** read (`#728`). The next bridge probe then reconciled that newer picture with the older single-read branch: **`memory_info() -> cpu_percent()`** with **memory-info-only emission** still recovered **`0.6667`** (`#729`). The next seven consumer-level follow-ups then sharpened the downstream side of the story materially: removing only the visible training-side **`logger.log_scalar("percent", stats.cpu_percent, ...)`** call on top of the bad `#728` worker still failed at **`1.0`** (`#730`); keeping that same bad worker/payload while stopping the training loop from **reading or logging the monitor payload at all** weakened it to **`0.8333`** (`#731`); keeping the full training-side **read path** but removing **all** payload logging still failed at **`1.0`** (`#732`); reducing the consumer further to only **event-proxy `is_set()/clear()`** with **no shared-value fetch** recovered **`0.6667`** (`#733`); adding back only the raw **shared-value fetch** **after** that clear restored **`1.0`** (`#734`); doing that same raw fetch **without clearing first** weakened only to **`0.8333`** (`#735`); and doing the raw fetch **first** and only then **clearing** the event also weakened only to **`0.8333`** (`#736`). So the best live monitor/process clue is no longer “remove multiprocessing entirely”; it is more specifically **remove or isolate the CPU/GPU monitor subprocesses while keeping the broader manager/process surface intact**. But the sign of the asymmetry now depends on the surface: on copied/wrapper mixed current-script surfaces the **GPU monitor** remains the harsher degradant, while on the detached historical **old-train-only** route the **CPU monitor** currently looks harsher, its **worker's spawned bootstrap** still looks like the sharpest lever, **manager-only** changes now look much weaker than the various worker-side combinations, the newer delay-start failures say this detached CPU damage is **not** just a simple early-start timing quirk, `#708` says it is **not** well explained by the spawned CPU worker merely talking through manager-backed proxies, and `#710-#745` now say the much worse raw CPU-only class needs **real memory/process inspection of the training process** rather than just spawned-child presence, transport overhead, `cpu_percent()` alone, `memory_info()` alone, `memory_percent()` alone, the already-tested `cpu_percent()+memory_percent()` and `memory_info()+memory_percent()` pairs, or a trivial one-call `cpu_percent()` warmup. The striking new point is that **`cpu_percent() + memory_info()`** is still the only parent-only pair that ever broke into the newer `0.5` class at all — but only on the original single-read **`memory_info() -> cpu_percent()`** branch (`#718-#720`). Inside the newer **two-cpu-call** variants, the stronger split is now about **which `cpu_percent()` read is actually emitted**: emitting the **second** post-`memory_info()` reading still recovers `0.6667` (`#725`), while emitting the **first** reading for the interval collapses to `1.0` on both call-order surfaces (`#727`, `#728`). And `#729` now shows that a **single** post-`memory_info()` `cpu_percent()` touch is **not** enough to poison the branch if that value is **not emitted**. On the consumer side, `#730-#739` now say the harmful downstream path is **deeper than the visible scalar logging calls**, does **not** require the remaining memory/child logs, and is now concentrated around **actual shared-state reads** rather than the event clear alone: under manager proxies, **event clear alone** is compatible with the stronger class, **clear then raw fetch** restores the bad class, and raw fetch either **without** a prior clear or **before** the clear weakens only to `0.8333`; under direct shared memory, **clear-only** still recovers `0.6667`, but **any actual payload read** stayed at `1.0` regardless of clear/fetch order (`#737-#739`). So the remaining explanation now looks narrower still: a weaker harm comes from merely touching `cpu_percent()` before `memory_info()`, the harsher bad class now points much more strongly at a real downstream consumer of the **first emitted `cpu_percent()` value** for the interval, and the remaining process-layer mystery is centered on the **shared-state read/synchronization semantics** themselves rather than on `SyncManager` RPC alone.
- So the current blocker is no longer just config-space exhaustion. There is now strong evidence that the benchmark surface on current HEAD/toolchain state has drifted away from the earlier `9cea09c` regime in a way that breaks the old reproducible backbone.
- A further external-state clue appeared during drift isolation: if the legacy `tokenized/v4` and `tokenized/default` caches are removed, **fresh tokenized rebuilds are currently impossible** on the current stack because `datasets.load_dataset("keithito/lj_speech", ...)` now fails with `RuntimeError: Dataset scripts are no longer supported, but found lj_speech.py`. So the benchmark currently depends on preserved legacy tokenized caches unless an older `datasets` stack is reconstructed.
- The cache-provenance picture is now much sharper: older `datasets` cannot read the **current** tokenized cache format at all, current-format tokenized caches regenerate only the current-style failing train surface, old-format tokenized caches regenerated under current `datasets` still fail, but the old train cache by itself is now strongly alive. So the runtime-critical cache difference may live more in the **train Arrow content / downstream train-stage serialization** than in tokenized-stage provenance alone.
- Direct payload comparison now supports that interpretation strongly: under the same cache key name `train/a009c9c8eee13c4a_e4146c08`, the old and current train Arrow payloads are not just slightly different — they have different row counts (`12365` vs `12315`), different fixed padded shapes (`156/122` vs `155/121`), and even the first 1000 row content hashes had **zero positional matches and zero set overlap** in a quick signature check. So the benchmark-drift problem now appears to include a major hidden **train-cache content drift under reused cache-key names**.
- The first real recovery suggested the missing ingredient was not any single package in isolation but the **combined older data/HF environment surface plus older-format cache rebuild**. That clue later softened on hybrid reruns, but the newer factor-isolation results sharpen it again: the path becomes meaningfully more alive when the detached `9cea09c` worktree is forced to be truly local on `PYTHONPATH`, the older HF stack is kept, and the cache surface preserves the old train payload. The old-`datasets` runtime package now looks much less central than the **older HF stack plus old train-cache provenance**, although the train-only surface is still sharply bimodal.
- The newest code-surface split tests now point to a more specific import/runtime story than “current script good, old `xax` required.” The live mixed surface does **not** require the exact current script path, the current repo root on `PYTHONPATH`, or even a split-vs-unified choice for the old root itself; copied current scripts can still be alive under either the split overlays or the unified detached-worktree root. The previous idea that fallback `task_module` identity alone explained the behavior is now weakened: even a hard copy named `/tmp/examples.ljspeech.py` can still hold `0.8333`. The strongest current clue is that **symlinked entrypoints are materially worse, the default `spawn` multiprocessing context is worse than `fork`, and the live culprit is no longer a generic subprocess story but a specific mix of spawned children on the symlinked surface**. Removing the manager helped the symlinked surface but was noisy (`0.6667 -> 0.6667 -> 0.8333`), and a cleaner manager-only isolation showed that **spawn manager-only** stayed at `1.0` while **fork manager-only** improved to `0.6667 -> 0.8333`. Forking the remaining CPU worker partly rescues its failure, and both the spawned **CPU worker** and the spawned **manager child** now directly show that they bootstrap through **whatever current entry script is `__main__`**: on the bad surface that is **`/tmp/current-copy-link.py`**, while on the hard-copied surface it is **`/tmp/current_ljspeech_copy.py`**. The newest causal tests sharpen this further in seven directions: forcing a copied surface to *present the symlink path* as `__main__` is enough to poison it back to `1.0`, removing the `ProcessMixin` child layer from that same forced-symlink surface partly rescues it to `0.8333`, and merely switching that forced surface to `fork` does not. A cheap minimal repro showed why the in-process repair is incomplete: rewriting `__main__.__file__` / `argv0` inside the script does **not** change the parent process's already-fixed loader/code provenance (`__loader__.path`, `co_filename`). An even stronger wrapper-based repair also failed: launching through an exec-wrapper that set a real `SourceFileLoader`, rebound `__main__` to the copy path, and `exec`-compiled the benchmark code with `co_filename=/tmp/current_ljspeech_copy.py` still stayed at `1.0`, while the plain copied-script control rerun immediately after still hit `0.8333`. A new deeper manager-child inspect pair then sharpened the bad-symlink side further still: on the symlink-vs-copy **spawn-manager-only** surface, the paired dumps matched on `_path_to_module_name(...)`, visible `sys.path`, and the absence of `examples.ljspeech`, but the raw **`main_file` / `inspect.getfile(__main__)`** path and the **`co_filename`** of main-defined functions changed in lockstep (`/tmp/current-copy-link.py` vs `/tmp/current_ljspeech_copy.py`). That makes the symlinked split look even less like a module-name-resolution problem and even more like a **raw main-module / code-object filename identity** problem. But the wrapper-side subprocess map is now both sharper **and** more split from the bad-symlink story: that wrapper surface jumps to **`0.5`** under `disable_multiprocessing=true`, to **`0.6667`** when **both monitor subprocesses** are disabled, and to **`0.6667`** again when the **GPU monitor is the only active monitor** but both the **manager and GPU worker are forced off spawn**. By contrast, no-`SyncManager` alone still fails, **GPU-only** under spawn still fails at `1.0`, forcing only the **GPU worker** off spawn weakly rescues only to `0.8333`, and forcing the **CPU worker** off spawn does not materially help its weaker `0.8333` class. The wrapper-vs-copy controls then sharpen the interpretation yet again: on the exec-real wrapper surface, both the spawned **GPU worker** (`#647`) and the spawned **manager child** (`#648`) already come up with **`/tmp/current_ljspeech_copy.py`** as `argv0` / `main_file`, the matching **true copied-script controls** (`#649`, `#650`) show **effectively identical visible child payloads** for those same subprocess types, the new **`multiprocessing.spawn` prep-data dumps** (`#651`, `#652`) show **effectively identical visible parent `get_preparation_data(...)` and child `prepare(...)` payloads**, and the even deeper visible **`Popen._launch` / `spawnv_passfds` / reduction-dump** metadata (`#653`, `#654`) also match up to run-specific values. So the remaining wrapper-specific harm is now **not** well explained by any visible child-bootstrap, visible `multiprocessing.spawn` prep-data, or visible parent launch-metadata difference at all. The remaining drift is therefore best understood as **two related but not identical mechanisms**: a **symlink/main-module + spawned-child bootstrap interaction** on the bad symlinked current-script surface, now further narrowed toward **raw main-file / `co_filename` identity**, plus a more distinct **outer wrapper / parent-bootstrap or still-hidden parent-side state + monitor-layer interaction** on the exec-real wrapper surface, rather than just a simple “swap deeper package files” story.
- The first concrete external-artifact pinning probe is now partly pruned: the older Qwen upload snapshot is **not** the missing ingredient. Its tokenizer/model files rebuild the exact same aligned normalized train/unpadded Arrow payloads as current main, and its model weights hash identically too.
- A new library-drift clue is now stronger: the historical benchmark-bearing commit `9cea09c` predates `uv.lock` entirely, while the current repaired environment is effectively a much newer manually restored package stack (for example `jax/jaxlib 0.8.2`, `transformers 4.57.3`, `tokenizers 0.22.1`, `datasets 4.4.2`). The newer-vs-older `datasets` probes confirm that package provenance really does change the cache/data surface, but `datasets` alone is not sufficient to recover the old `0.6667` behavior. A modestly older HF stack also changes generation behavior without recovering the old backbone, so the remaining live drift hypothesis now points more toward **multi-package / broader environment interactions** than toward any single obvious package lever.

## What Has Been Pruned
- Sampled grouped 2-head backbone: third probe collapsed
- Normalized exact-last 9-token side path: third probe collapsed
- Mixed-text aligned 4-head grouped fuller-LoRA fallback: third probe collapsed
- Aligned mixed-text grouped 2-head branch: collapsed
- Diff-targeted sampled grouped 2-head branch: collapsed
- Aligned normalized exact-last 12-token center: collapsed
- Broader plain grouped `semantic_future_prediction_steps=3` + `semantic_block_decode_size=4`: failed cleanly
- Post-alignment heavy-eval plain-vs-exact-last reranker on the robust 4-head grouped backbone: only tie-level `0.6667` and much slower

## Best Next Directions
1. Treat **benchmark-surface stabilization** as the immediate priority, not more local model retunes on the drifting current HEAD.
2. Keep using the aligned normalized 4-head grouped backbone as the main diagnostic probe while isolating repo/toolchain drift.
3. The most important near-term control is now **import-surface fidelity**: when probing the historical detached worktree, explicitly put `/tmp/xax-bench-9cea09c` on `PYTHONPATH` so the run actually uses the old `xax/...` package code too, not just the old top-level example script.
4. Inside that fully local detached-worktree surface, keep the **older HF stack** fixed. The old-`datasets` runtime package now looks less central.
5. The strongest current live path is still the **old train cache alone**, but its picture has changed materially: the raw route stayed bimodal / unstable, the **fully local old-train-only + no-stats** branch weakened from **`0.6667 -> 0.5 -> 0.5`** to **`0.8333`** on the fourth probe, the completed **CPU-only + forked manager + forked CPU worker** split reached **`0.3333`** only once, and the newer detached CPU-only ablations now say the damage is **not** just an early-start timing issue, **not** just manager-proxy transport, **not** just spawned-child presence, **not** just `cpu_percent()` alone, **not** just `memory_info()` alone, **not** just `memory_percent()` alone, and **not** even the tested **`cpu_percent() + memory_percent()`** or **`memory_info() + memory_percent()`** pairs. The one remaining parent-only pair, **`cpu_percent() + memory_info()`**, is still the strongest isolated clue because it reached **`0.5`** once, but its exact sequence is now **`0.5 -> 0.6667 -> 1.0`**, a trivial one-call **`cpu_percent()` warmup** only weakens it to **`0.8333`**, reversing the per-interval order to **`cpu_percent() -> memory_info()`** collapses it cleanly to **`1.0`** even with warmup, and the newer follow-ups now sharpen the split further: the only clearly stronger two-read branch is **`memory_info() -> cpu_percent() -> cpu_percent()`** when the emitted value is the **second** post-`memory_info()` read (`#725`), while emitting the **first** `cpu_percent()` reading for the interval collapses to **`1.0`** on both call-order surfaces (`#727`, `#728`), and a single post-`memory_info()` `cpu_percent()` touch with **memory-only emission** still recovers **`0.6667`** (`#729`). The consumer-path follow-ups then narrowed the downstream side even further: removing only the visible `logger.log_scalar("percent", stats.cpu_percent, ...)` call still failed at **`1.0`** (`#730`), stopping the training loop from reading/logging the monitor payload entirely weakened the same bad surface only to **`0.8333`** (`#731`), keeping the full read path but removing **all** logging still failed at **`1.0`** (`#732`), reducing the consumer to only **event-proxy `is_set()/clear()`** recovered **`0.6667`** (`#733`), adding back only the raw **`ValueProxy.get()`** after that clear restored **`1.0`** (`#734`), and both **get-without-clear** (`#735`) and **get-then-clear** (`#736`) weakened only to **`0.8333`**. The next eighteen transport/synchronization follow-ups then sharpened that clue further: keeping the same bad minimal consumer but swapping the CPU monitor itself to direct **`context.Event()` + shared `context.Value(...)`** transport still failed cleanly at **`1.0`** for **clear-then-fetch** (`#737`), and also stayed at **`1.0`** for **fetch-then-clear** (`#738`), while the matching direct-shared-memory **clear-only** consumer first recovered **`0.6667`** and then improved further to a reproduced **`0.5 -> 0.5`** (`#739`, `#752`, `#753`). More importantly, the direct shared-memory path then showed that the harsh class does **not** even require payload copying, and now does not look lock-specific or raw-attribute-specific either: **clear then lock-only** with **no field reads at all** still failed at **`1.0`** (`#740`), the original **lock-only without clearing first** (`#741`) and **lock-then-clear** (`#742`) initially weakened to **`0.8333`**, but the exact rerun of that last apparent no-clear softened lock-touch corner later collapsed back to **`1.0`** (`#751`), **clear then `get_lock()` without actually acquiring it** still failed at **`1.0`** (`#743`), **clear then a bare `_lock` attribute touch** also failed at **`1.0`** (`#744`), that same bare `_lock` attribute touch **without** clearing first still failed at **`1.0`** (`#745`), a bare **`_obj` attribute touch** likewise failed at **`1.0`** both **with** and **without** prior clear (`#746`, `#747`), the public **`get_lock()`** accessor still failed at **`1.0`** on the **no-clear** branch (`#748`), direct wrapper-bound **`acquire()/release()`** also failed at **`1.0`** (`#749`), and even the wrapper's own **`with monitor._cpu_stats_smem:`** context-manager path still failed at **`1.0`** (`#750`). The new mixed clear-only transport splits then isolate that live branch more sharply: the matching **manager clear-only** rerun failed at **`1.0`** (`#754`), keeping the manager-backed shared-value path but swapping only the **events** to direct `context.Event()` also failed at **`1.0`** (`#755`), keeping the **direct shared-value path** but swapping the events back to manager proxies improved materially to **`0.6667 -> 0.8333`** (`#756`, `#757`), then making only **`monitor_event` direct** on top of that still landed at **`0.8333`** on first probe (`#758`), removing the worker-side **manager `ValueProxy.set(...)`** call entirely from the **direct-Event + manager-Value** branch still only softened it to **`0.8333`** (`#759`), and then removing the worker-side **direct sharedctypes writes** from the **manager-Event + direct-Value** branch also still landed at **`0.8333`** (`#760`), while removing those same direct writes from the **fully direct Event+Value** branch knocked it down from **`0.5`** to **`0.8333`** (`#761`). The next write-side splits then sharpened that still further: on the **fully direct Event+Value** branch, **worker-side lock-only** without field stores still stayed at **`0.8333`** (`#762`), **constant zero writes** also still stayed at **`0.8333`** (`#763`), **changing synthetic counter writes** into all sharedctypes fields first recovered a stronger **`0.6667`** class (`#764`) but then collapsed back to **`1.0`** on exact rerun (`#768`), a tiny **alternating all-fields 0/1 toggle** (`#765`) failed at **`1.0`**, a **same increasing counter in every field** (`#766`) also failed at **`1.0`**, and a **constant but per-field-distinct pattern** (`#767`) failed at **`1.0`** too. Two real-payload subset controls then closed the sharper remaining corner: writing only the **real memory-info-derived fields** while zeroing the rest still failed at **`1.0`** (`#769`), and writing only the **real `cpu_percent` field** while zeroing the rest also failed at **`1.0`** (`#770`). A further code-path split then tested whether the direct branch's earlier lift might depend on going through the synchronized wrapper's per-field property setters rather than just ending up with the same bytes: writing the full real payload directly through **`stats.get_obj()`** under the outer lock also failed at **`1.0`** (`#771`), but the contemporaneous exact rerun of the original full-real wrapper-property branch also failed at **`1.0`** (`#772`), so that raw-vs-wrapper write-path split was initially inconclusive. Two additional real-field combinations then filled in the next obvious subset corners: adding a real **`mem_percent`** to the **memory-info-only** subset still failed at **`1.0`** (`#773`), adding a real **`mem_percent`** to the **cpu-percent-only** subset softened only to **`0.8333`** (`#774`), and adding real **`mem_percent`** to the full real `memory_info + cpu_percent` write set still failed at **`1.0`** (`#775`). The next eighteen sharper real-subset/code-path probes then narrowed the surviving clue substantially: keeping only the non-duplicated real **`cpu_percent + mem_rss + mem_vms + mem_shared`** fields still failed at **`1.0`** (`#776`); keeping only the later duplicated total fields **`cpu_percent + mem_rss_total + mem_vms_total`** recovered **`0.5`** once and then weakened to **`0.8333`** on exact rerun (`#777`, `#782`); keeping only **`cpu_percent + mem_rss_total`** or only **`cpu_percent + mem_vms_total`** each weakened to **`0.6667`** (`#778`, `#779`); keeping the late total-field pair **without** a real `cpu_percent` failed cleanly at **`1.0`** (`#780`); writing that sharpened late-pair subset through **`stats.get_obj()` raw stores** also failed at **`1.0`** (`#781`); swapping the late-pair property-store order to **`mem_vms_total` before `mem_rss_total`** weakened to **`0.6667`** (`#783`); moving the `cpu_percent` property write **after** the late pair also weakened to **`0.6667`** (`#784`); removing the **outer batch-held `with stats.get_lock():`** while keeping the same broader wrapper-property field/value/order path weakened to **`0.8333`** (`#785`); shrinking the worker all the way down to only the three ordered wrapper-property stores **`cpu_percent -> mem_rss_total -> mem_vms_total`** while **keeping** that outer batch-held lock recovered **`0.1667`** once (`#786`); the exact rerun of that same minimal batch-held triple weakened back to **`0.8333`** (`#787`); removing the outer batch lock from that same minimal ordered triple collapsed cleanly to **`1.0`** (`#788`); keeping that same minimal triple under the outer batch-held lock but writing through **`stats.get_obj()` raw stores** softened only to **`0.6667`** (`#789`); trying to mimic the wrapper-property reentrant lock churn manually around those same raw stores still collapsed to **`1.0`** (`#790`); adding a single extra adjacent-field property write **`mem_percent=0.0`** to that minimal triple also collapsed it to **`1.0`** (`#791`); adding a single extra far-offset trailing property write **`num_child_procs=0`** likewise collapsed it to **`1.0`** (`#792`); even a single extra **duplicate setter invocation** on an already-live field weakened it back to **`0.8333`** whether the duplicate was the final **`mem_vms_total`** store (`#793`) or the first **`cpu_percent`** store (`#794`); keeping the same nested acquire/release churn but doing a **fresh `stats.get_obj()` lookup inside each nested critical section** still failed cleanly at **`1.0`** (`#795`); switching only that nested raw-write object-access path from **`stats.get_obj()`** to direct private **`stats._obj`** recovered the weaker **`0.6667`** class (`#796`); directly calling the generated setter bodies via **`fset(...)`** still failed at **`1.0`** (`#797`); even direct property-descriptor calls via **`__set__(...)`** also failed cleanly at **`1.0`** (`#798`); built-in **`setattr(...)`** weakened only to **`0.8333`** (`#799`); direct **`object.__setattr__(...)`** recovered the stronger partial **`0.5`** class once (`#800`); the exact rerun of that same **`object.__setattr__(...)`** branch then collapsed cleanly back to **`1.0`** (`#801`); replacing the outer batch hold with direct wrapper-bound **`stats.acquire(); ...; stats.release()`** also collapsed cleanly to **`1.0`** (`#802`); replacing it with the wrapper's own **`with stats:`** context-manager path softened only to **`0.8333`** (`#803`); moving the same three plain assignments into a module-level helper called inside the original **`with stats.get_lock():`** also collapsed cleanly to **`1.0`** (`#804`); and prebinding the raw lock with **`lock = stats.get_lock(); with lock:`** recovered **`0.6667`** once (`#805`) before weakening to **`0.8333`** on exact rerun (`#806`). So the live explanation is now narrower than simple manager-proxy transport, simple ordering alone, payload reads, or even lock acquisition itself: the remaining bad direct-shared-memory class is centered on the interaction between **event consumption** and certain subsequent **synchronized-wrapper touches** around that first-emitted `cpu_percent()` payload. The manager-proxy/object-copy path still softens some read-order variants to `0.8333`, but the new clear-only transport map now says the rescue depends **primarily on the direct shared-value/sharedctypes path**, with direct Event transport alone not helping, the mixed manager-Event/direct-Value route helping some but remaining noisy, and the fully direct Event+Value combination still being the strongest branch. More specifically, the new best write-side clue now appears to need more than merely the absence of manager proxies: **lock touch alone** is not enough, **constant direct stores** are not enough, **time variation alone** is not enough, **per-field heterogeneity alone** is not enough, the earlier synthetic `0.6667` branch is now downgraded as non-reproducible, neither the **real memory fields alone** nor the **real `cpu_percent` field alone** is sufficient, adding **`mem_percent`** to those reduced subsets is still not enough, the broader zero-fill late-total path is weaker/noisier than first thought, raw final bytes under the outer batch-held lock soften only to `0.6667`, and the newest controls now say the failed manual raw paths are **not** just generic nested-lock churn either: nested reacquire/release can still reach the weaker `0.6667` class once the stores go through direct private **`_obj`**, but nested raw paths that go through **`get_obj()`** stay at `1.0`; direct **`fset`** calls and explicit property **`__set__`** calls also stay at `1.0`; built-in **`setattr(...)`** is only partially helpful at `0.8333`; **`object.__setattr__(...)`** is downgraded to a noisy clue after its `0.5 -> 1.0` sequence; moving the same plain assignments into a helper code object also fails; rebinding only the synchronized wrapper to a local alias **`s = stats`** while keeping the original inline **`with stats.get_lock():`** and the same three plain stores recovers `0.5` once and then weakens to `0.8333`; moving only the **`get_lock()`** receiver onto a local alias while keeping the plain **`stats.*`** stores recovers a reproducible `0.6667 -> 0.6667`; prebinding only the bound **`get_lock`** method locally also stays in that same `0.6667` class; and even the outer batch-held lock surface is narrower than “same lock held somehow,” because direct wrapper-bound **`acquire()/release()`** collapses to `1.0`, **`with stats:`** only softens to `0.8333`, and even a prebound raw-lock context **`lock = stats.get_lock(); with lock:`** weakens on exact rerun. The newest source-expression splits then sharpen the lock-held write surface further: pulling **both** late-field value computations **`int(mem_info.rss)`** and **`int(mem_info.vms)`** out of the lock weakens to `0.8333`; pulling only the **final** late-field computation **`int(mem_info.vms)`** out of the lock collapses to `1.0`; pulling only the **middle** late-field computation **`int(mem_info.rss)`** out of the lock still preserves `0.6667`; moving only the raw **`mem_info.vms`** attribute load out of the lock while keeping **`int(...)`** inside still collapses to `1.0`; moving only the raw **`mem_info.rss`** attribute load out of the lock while keeping **`int(...)`** inside also collapses to `1.0`; once the middle-field pre-lock local already holds the fully computed integer **`mem_rss_total = int(mem_info.rss)`**, even re-wrapping it as **`int(mem_rss_total)`** inside the lock still preserves `0.6667`; the analogous final-field variant **`mem_vms_total = int(mem_info.vms)`** outside and then **`int(mem_vms_total)`** inside still collapses to `1.0`; and the newest inside-lock local-temp controls now show that even when the computation stays inside the **same lock and same code object**, splitting either live memory-field line into a local-compute statement followed by a wrapper-property store statement weakens to `0.8333` for both the **final** `mem_vms_total` case (`#820`) and the **middle** `mem_rss_total` case (`#821`). That means the remaining gap is now narrower still: the stronger spike still looks specific to the actual synchronized wrapper-property **instance attribute assignment** path under the original **inline `with stats.get_lock():`** hold, the weaker partial rescue is compatibly explained by raw **private `_obj`** stores, the **`get_obj()` accessor path itself** still looks like part of the toxic manual-raw branch, and the newest evidence says the live clue now includes a two-part source-expression story: **both** live memory-field stores dislike being split into a local-then-`STORE_ATTR` two-statement path inside the lock, while the **final `mem_vms_total`** path is also much less movable across the lock boundary than the **middle `mem_rss_total`** path. So the helpful real payload is now much more plausibly tied to a very specific **exact outer-batch-held ordered synchronized-wrapper property-store triple** over **real `cpu_percent` plus the paired later `mem_rss_total` / `mem_vms_total` stores** than to the earlier `mem_rss` / `mem_vms` / `mem_shared` core, the broader zero-fill path, fewer writes alone, raw final bytes alone, generic nested lock churn, local offset effects, approximate field-set/order matching, more `get_obj()` freshness/caching variations, more blind attribute-setting API rewrites, more blind outer-lock-hold API rewrites, helper-code-object relocation of the same plain assignments, or the literal local variable name used at the STORE_ATTR sites. Immediate priority is therefore now less about broad reruns and more about explaining the **shared-state synchronization / wrapper-touch semantics themselves**, especially **manager `ValueProxy.set(...)` vs direct sharedctypes writes**, why **`get_obj()`** is materially harsher than direct private **`_obj`** on the nested raw-write path, why the true **instance attribute assignment + inline `with stats.get_lock():`** surface behaves differently from built-in **`setattr`**, direct **`object.__setattr__`**, explicit **`fset`** / **`__set__`**, helper-code-object relocation of the same assignments, local-wrapper-alias relocation of the same assignments, direct **`acquire()/release()`**, and both the wrapper's own **`with stats:`** path and a prebound raw-lock **`with lock:`** path on this exact minimal late-field sequence, and now also why both live memory-field writes care about staying as **one-line wrapper-property store statements** while only the final **`mem_vms_total`** path additionally resists being moved across the lock boundary.
6. The sharpened train-cache-only factor isolation now says:
   - old HF stack still matters (`#583` failed cleanly at `1.0`)
   - local detached-worktree import fidelity still matters (`#584` fell back to the slow ~18k/10min hybrid regime)
   - the first **broad** multiprocessing toggles still looked weak on the detached old-train-only path (`#660` `disable_multiprocessing=true` only reached `0.8333`; `#661` `multiprocessing_context=fork` fell to `1.0`)
   - the narrower **no-stats** intervention is still more alive there than the broad toggles, but it is also noisy (`#680-#682`, `#693`)
   - and the detached old-train-only monitor split is now asymmetric in the **other** direction from the copied/wrapper mixed surfaces:
     - **GPU-only** weakens to `0.6667 -> 0.8333` (`#683`, `#685`)
     - **CPU-only** collapses to `1.0` (`#684`)
     - slowing the raw **CPU-only** monitor cadence to `cpu_stats.ping_interval=10` still fails at `1.0` (`#700`)
     - delaying that raw **CPU-only** spawned worker until **120s** still fails at `1.0` (`#706`)
     - delaying it until **~540s** also still fails at `1.0` (`#707`)
     - removing the CPU monitor's **manager-backed proxy transport** still fails at `1.0` (`#708`)
     - moving the same CPU-monitor logic fully **inline** also still fails at `1.0` and slows throughput badly (`#709`)
     - reducing the spawned CPU worker to a **dummy sleeping child** improves to `0.6667` (`#710`)
     - reducing it only to **parent-only `psutil` sampling** still fails at `1.0` (`#711`)
     - keeping only **zero-valued stats emission** improves weakly to `0.8333` (`#712`)
     - keeping only **`proc.cpu_percent()`** also improves weakly to `0.8333` (`#713`)
     - forcing the **CPU worker** off spawn on that CPU-only route improves it reproducibly to `0.8333 -> 0.8333` (`#686`, `#687`)
     - forcing only the **manager** off spawn on that CPU-only route does **not** help (`#688`)
     - forcing the **GPU worker** off spawn on the GPU-only route does **not** help either (`#689`)
     - moving **both** the **manager** and the **CPU worker** off spawn on that CPU-only route then improves sharply to `0.3333` (`#690`)
7. The best-controlled high-signal routes are now:
   - **local detached `9cea09c` worktree on `PYTHONPATH` + old HF stack + old train cache**, especially the new **no-stats** branch and its CPU/GPU monitor split
   - **bad symlinked current-script surface + old-code namespace surface + old HF stack + aliased old full caches**, especially with subprocess controls:
     - `multiprocessing_context=fork` improved it to **`0.8333`**
     - **no-`SyncManager`** patched ProcessMixin gave **`0.6667 -> 0.6667 -> 0.8333`**
     - **manager-only under `fork`** (both stats monitors disabled) gave **`0.6667 -> 0.8333`**, versus spawn manager-only at **`1.0`**
     - **forked manager + CPU-only spawned worker** failed at **`1.0`**, but improved to **`0.8333`** once that CPU worker was also forced to `fork`
     - **forked manager + GPU-only spawned worker** weakened only to **`0.8333`**
     - **all-forked children** (manager + CPU + GPU) gave **`0.6667 -> 1.0`**
     - direct bootstrap introspection now shows both the spawned **manager child** and spawned **CPU worker** see the symlinked entry script as `__main__`, while their copied-surface counterparts see the hard copy instead
     - forcing a copied surface to present the **symlink path** as `__main__` is enough to poison it back to **`1.0`**
     - removing the `ProcessMixin` child layer from that same forced-symlink copied surface partly rescues it to **`0.8333`**
     - but merely switching that forced-symlink copied surface to `multiprocessing_context=fork` does **not** rescue it
     - and rewriting `__main__.__file__` / `argv0` from *inside* an already-symlink-launched process does **not** rescue it either
     - even a stronger **exec-real wrapper** that canonicalizes loader/path/code provenance before any benchmark imports still failed at **`1.0`**, while the plain copied-script control rerun still hit **`0.8333`**
     - the broader **`disable_multiprocessing=true`** rescue class is now downgraded on these mixed current-script surfaces:
       - copied current-script: `0.5 -> 1.0` (`#608`, `#666`)
       - direct exec-real wrapper: `0.5 -> 1.0` (`#638`, `#667`)
     - removing **only** the `SyncManager` on that wrapper surface did **not** help; it stayed at **`1.0`**
     - disabling **both** wrapper-side monitor subprocesses now sits at **`0.6667 -> 0.8333`** (`#640`, `#668`)
     - the matching **hard-copied current-script + no-stats** control is now also live at **`0.6667 -> 0.8333`** (`#669`, `#670`)
     - but trying to make that copied no-stats surface *cleaner* or *narrower* did **not** help:
       - unified old-root no-stats control failed at **`1.0`** (`#678`)
       - copied no-stats + **old-train-only** cache route also failed at **`1.0`** (`#679`)
     - on the hard-copied current-script surface itself, **GPU-only** monitor lands at **`0.8333`** (`#671`), **GPU-only + forked manager** still lands at **`0.8333`** (`#674`), **GPU-only + forked manager + forked GPU worker** improved once to **`0.6667`** then collapsed to **`1.0`** (`#675`, `#676`), **GPU-only + forked GPU worker only** failed at **`1.0`** (`#677`), and **CPU-only** improved once to **`0.5`** then collapsed to **`1.0`** (`#672`, `#673`)
     - wrapper-side **GPU monitor only** still failed at **`1.0`**, while **CPU monitor only** weakened only to **`0.8333`**
     - making the wrapper-side manager use **`fork`** does **not** rescue that GPU-only failure
     - forcing the wrapper-side **GPU worker itself** off spawn rescues that GPU-only surface to **`0.8333`**, and combining that with a **forked manager** rescues further to **`0.6667`**
     - forcing the wrapper-side **CPU worker itself** off spawn does **not** materially improve its weaker `0.8333` class
     - new direct wrapper-side bootstrap dumps now show that the spawned **GPU worker** and spawned **manager child** already see **`/tmp/current_ljspeech_copy.py`** as `argv0` / `main_file`, the matching true copied-script controls show **effectively identical visible child payloads** for those subprocesses, the visible `multiprocessing.spawn` prep-data dumps also match, and the deeper visible `Popen._launch` / `spawnv_passfds` metadata now match too
   - the copied-script mixed surfaces remain live too, but the strongest mechanistic signal is now split between the bad symlinked path and a distinct wrapper/bootstrap surface
8. The next nontrivial step should now focus on the **spawned child bootstrap** itself, plus the small remaining **parent-side pre-bootstrap surface** and the new **outer wrapper/bootstrap identity** clue: the highest-value probes now are no longer generic reruns, but direct explanation. We now know the spawned **CPU worker** and spawned **manager child** both come up with `__main__=/tmp/current-copy-link.py` and `sys.path` beginning with `/tmp` on the bad symlinked surface, while their copied-surface counterparts instead see `__main__=/tmp/current_ljspeech_copy.py`; we now also know that on the cleaner spawn-manager-only comparison, the visible child surfaces already match on `_path_to_module_name(...)`, `sys.path`, and the absence of `examples.ljspeech`, while the raw **`main_file` / `inspect.getfile(__main__)`** path and main-function **`co_filename`** still differ in lockstep. We also now know that the simplest immediate ablation — merely touching one main-defined function's `co_filename` in the manager child — is **not** enough to reproduce the `#656` rescue, and the full `#656` overlay itself also failed its exact rerun. The newer temp-script canonicalization probe is now downgraded too: after one `0.8333` on a freshly rebuilt temp-script cache surface, both the **direct temp-script control** and the **symlinked rerun** collapsed back to `1.0`, so that branch is not a stable proof either. And we already know that *forcing* a copied surface to present the symlink path is enough to poison it, while trying to canonicalize the symlinked process from inside is not enough to rescue it. A cheap minimal repro further shows that in-process rewriting does **not** change the parent process's already-fixed loader/code provenance (`__loader__.path`, `co_filename`). The wrapper probes now sharpen the split rather than collapsing it: even canonicalizing the benchmark code itself is not enough if the outer launch surface still differs; the broader `disable_multiprocessing=true` rescue class has now collapsed on exact reruns; the narrower **no-stats** intervention remains more alive on both wrapper and copied surfaces; on the wrapper surface removing only the `SyncManager` still does **not** help; inside the monitor layer the **GPU monitor path** remains the harsher remaining culprit; and the newest wrapper-vs-copy controls show that for both the **GPU worker** and the **manager child**, not only the child's visible `__main__` / `sys.path` payload but also the visible `multiprocessing.spawn` prep data and deeper visible `Popen._launch` / `spawnv_passfds` metadata are already effectively the same on wrapper and true-copy surfaces. The detached old-train-only route now adds a useful contrast class instead of a dead end: the **no-stats** branch is still more alive than the broad toggles but has weakened from `0.6667 -> 0.5 -> 0.5` to `0.8333`, its monitor split still says the **CPU monitor** is harsher than the **GPU monitor**, the CPU-side factor isolation still says that harsher detached-route CPU damage is tied more to the **CPU worker's spawned bootstrap** than to manager spawn alone, the newer timing probes now say this is **not** rescued by polling less often, **not** rescued by delaying the CPU worker until after early bootstrap, and **not** rescued even when that CPU worker appears only near the very end of training, and the proxy/control follow-ups now say it is **not** rescued by merely removing **manager-backed proxy transport** from the CPU monitor and **not** cleanly rescued by moving the same CPU-monitor logic inline either. The newest split is sharper still: the bad class is **not** explained by spawned-child presence alone (`#710`), **not** explained by transport alone (`#708`, `#712`), **not** dependent on recursive child-process scans (`#711`), and **not** driven by `proc.cpu_percent()` alone (`#713`). The remaining harsher detached CPU route now looks tied mainly to the CPU worker's **memory/process-inspection `psutil` path**, with proxy/event/logging traffic acting as a weaker secondary harm. So the next best step is now to explain which downstream consumers care specifically about the **raw main-file / `inspect.getfile(__main__)` / function-`co_filename` identity** on the bad symlinked surface, which care about **still-hidden parent-side state beyond all launch metadata surfaced so far** on the wrapper surface, and why the detached historical old-train-only route flips the monitor asymmetry toward the **CPU worker spawn path** with an added **manager-spawn interaction** — not to spend many more runs on already-mapped module-name or visible launch-metadata scans, on more detached **CPU-start-timing** sweeps, or on more small transport rewires of the same raw CPU-only branch.
9. Broader JAX/CUDA reconstruction remains live, but the explicit `jax 0.9.2` stack is currently too new / source-incompatible; prefer compatible lower-level probes over more local model retunes.
10. Avoid spending more runs on already-pruned noisy 2-head sampled / mixed / normalized-exact-last families unless a truly new mechanism appears.
11. Once the benchmark surface is stable again, resume structural exploration around grouped-prefix transition design rather than scalar retunes.
