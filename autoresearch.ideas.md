- **Top live blocker:** benchmark comparability drift remains unsolved, but the live dependency picture now looks increasingly like a **monitor shared-state read/synchronization + spawned-multiprocessing + symlinked-entrypoint** problem layered on top of the old-code namespace surface, not just a simple source-diff problem.
- **Canonical drift probe:** keep using the aligned normalized 4-head grouped-prefix backbone:
  - `text_source=normalized`
  - `semantic_future_prediction_steps=4`
  - `semantic_future_prediction_weight=0.5`
  - `semantic_self_condition_prob=0.5`
  - `semantic_eos_weight=2.0`
  - `semantic_block_decode_size=3`
  - `semantic_block_decode_max_grouped_tokens=12`
- **Best recorded overall remains fragile:** `0963536` at `asr_wer_prefix=0.1667` from the sampled exact-last `both_weighted_sampled` `3:1` 2-pass branch. Do not spend more runs there until benchmark comparability is trustworthy.
- **Historical reproducible anchor:** the aligned normalized 4-head grouped mainline held `0.6667` three times on the old benchmark-bearing surface and is still the right diagnostic target.
- **Still-live detached-worktree route:**
  - fully local detached `9cea09c` worktree on `PYTHONPATH`
  - old HF stack (`transformers 4.52.4`, `tokenizers 0.21.4`, `huggingface_hub 0.36.2`)
  - strongest simplified cache clue remains the old train cache alone
- **Train-cache clue still matters most:**
  - fully local worktree + old HF + **old train cache only** originally produced `0.8333 -> 0.3333 -> 1.0 -> 1.0 -> 0.8333`
  - the first broad process toggles on that detached old-train-only path still looked weak:
    - `disable_multiprocessing=true` only reached `0.8333` (`#660`)
    - `multiprocessing_context=fork` fell to `1.0` (`#661`)
  - but the newer **process-layer split** is now real, even if noisy:
    - fully local detached old-train-only + **no-stats**: `0.6667 -> 0.5 -> 0.5 -> 0.8333` (`#680-#682`, `#693`)
    - moving only the **manager** off spawn on that broader **no-stats** branch failed at `1.0` (`#692`)
    - reintroducing only the **GPU monitor** weakens the route to `0.6667 -> 0.8333` (`#683`, `#685`)
    - reintroducing only the **CPU monitor** collapses it to `1.0` (`#684`)
    - slowing the raw **CPU-only** monitor cadence to `cpu_stats.ping_interval=10` still failed at `1.0` (`#700`)
    - delaying the raw **CPU-only** spawned worker until **120s** into training still failed at `1.0` (`#706`)
    - delaying that same raw **CPU-only** spawned worker until **~540s** — i.e. almost the very end of the run — also still failed at `1.0` (`#707`)
    - keeping the raw **CPU-only** spawned worker but removing its **manager-backed proxy transport** (switching the CPU monitor itself to direct `ctx.Event()` / `ctx.Value(...)`) still failed at `1.0` (`#708`)
    - moving the same CPU-monitor logic fully **inline on the training process** also still failed at `1.0` and slowed throughput sharply (~26.6k / 10 min) (`#709`)
    - keeping the spawned CPU worker but reducing it to a **dummy sleeping child** with no `psutil` work and no stats emission improved cleanly to `0.6667` (`#710`)
    - keeping the spawned CPU worker and stats emission but reducing it to **parent-only `psutil` sampling** still failed at `1.0` (`#711`)
    - keeping the spawned CPU worker and **zero-valued stats emission** with no `psutil` at all improved only to `0.8333` (`#712`)
    - reducing the worker further to **`proc.cpu_percent()` only** also improved only to `0.8333` (`#713`)
    - reducing the worker to **`proc.memory_info()` only** improved cleanly to `0.6667` (`#714`)
    - reducing the worker to **`proc.memory_percent()` only** improved only to `0.8333` (`#715`)
    - combining **`proc.cpu_percent()` + `proc.memory_percent()`** still improved only to `0.8333` (`#716`)
    - combining **`proc.memory_info()` + `proc.memory_percent()`** recovered the stronger `0.6667` class (`#717`)
    - combining **`proc.cpu_percent()` + `proc.memory_info()`** improved further to `0.5`, then weakened to `0.6667`, then collapsed to `1.0` on the third exact probe (`#718-#720`)
    - adding a one-call **`cpu_percent()` warmup** before that same `memory_info() -> cpu_percent()` loop weakened it to `0.8333` (`#721`)
    - reversing the per-interval order to **`cpu_percent() -> memory_info()`** collapsed cleanly to `1.0` both **without** and **with** that warmup (`#722`, `#723`)
    - touching **`cpu_percent()` first**, then doing **`memory_info()`**, then logging a **second** `cpu_percent()` weakened only to `0.8333` (`#724`)
    - doing **`memory_info()` first**, then touching **`cpu_percent()`**, then logging a **second** `cpu_percent()` recovered `0.6667` (`#725`)
    - touching **`cpu_percent()` first** and then keeping **`memory_info()` only** also weakened to `0.8333` (`#726`)
    - touching **`cpu_percent()` first**, then doing **`memory_info()`**, then touching `cpu_percent()` again but still logging the **first** `cpu_percent()` value collapsed cleanly to `1.0` (`#727`)
    - doing **`memory_info()` first**, then touching `cpu_percent()` twice but still logging the **first** post-`memory_info()` `cpu_percent()` value also collapsed cleanly to `1.0` (`#728`)
    - doing **`memory_info()` first**, then touching **`cpu_percent()` once** but still emitting **memory-info-only** stats recovered `0.6667` (`#729`)
    - removing only the visible training-side **`logger.log_scalar("percent", stats.cpu_percent, ...)`** consumer from that bad `#728` surface still failed at `1.0` (`#730`)
    - keeping that same bad `#728` worker/payload but stopping the training loop from **reading or logging the monitor payload at all** weakened it to `0.8333` (`#731`)
    - keeping the full training-side **read path** (`monitor.get*`, event clear, proxy read, cached-state update) but suppressing **all** payload logging still failed at `1.0` (`#732`)
    - reducing the consumer further to only **event-proxy `is_set()/clear()`** — with **no shared-value fetch** and no logging — recovered `0.6667` (`#733`)
    - adding back only the raw **shared-value fetch** **after** the event clear restored the full bad `1.0` class (`#734`)
    - doing the raw **shared-value fetch without clearing the event first** weakened only to `0.8333` (`#735`)
    - doing the raw **shared-value fetch first** and only then **clearing** the event also weakened only to `0.8333` (`#736`)
    - swapping that same bad minimal consumer to **direct `context.Event()` + shared `context.Value(...)` transport** while keeping **clear-then-fetch** still failed cleanly at `1.0` (`#737`)
    - under that same direct shared-memory transport, **fetch-then-clear** also still failed cleanly at `1.0` (`#738`)
    - under that same direct shared-memory transport, **clear-only** recovered the stronger class at `0.6667` and then improved further to a reproduced `0.5 -> 0.5` on exact reruns (`#739`, `#752`, `#753`)
    - the matching **manager-proxy clear-only** rerun failed cleanly at `1.0` (`#754`)
    - keeping the manager-backed shared-value path but swapping only the **events** to direct `context.Event()` still failed at `1.0` (`#755`)
    - while keeping the **direct shared-value path** but swapping the events back to manager proxies softened to `0.6667 -> 0.8333` (`#756-#757`)
    - and keeping the **direct shared-value path** while making only `monitor_event` direct (with `start_event` still manager-backed) also landed at `0.8333` on first probe (`#758`)
    - removing the worker-side **manager `ValueProxy.set(...)`** call entirely from the **direct-Event + manager-Value** clear-only branch still only softened it to `0.8333` (`#759`)
    - removing the worker-side **direct sharedctypes writes** from the **manager-Event + direct-Value** mixed branch also landed at `0.8333` (`#760`)
    - and removing those same direct writes from the **fully direct Event+Value clear-only** branch knocked it down from `0.5` to `0.8333` (`#761`)
    - keeping the **fully direct Event+Value clear-only** branch but reducing the worker to **lock-only with `with stats.get_lock(): pass`** also stayed at `0.8333` (`#762`)
    - keeping that same branch but doing only **constant zero writes** into every sharedctypes field also stayed at `0.8333` (`#763`)
    - while keeping the same direct branch but writing a **changing synthetic counter pattern** into every sharedctypes field recovered a stronger `0.6667` class once (`#764`), but the exact rerun collapsed back to `1.0` (`#768`)
    - a tiny **alternating all-fields 0/1 toggle** then failed cleanly at `1.0` (`#765`)
    - a **same-increasing-counter in every field** pattern also failed at `1.0` (`#766`)
    - a **constant but per-field-distinct pattern** (`1.0, 2.0, 3, 4, ...`) also failed at `1.0` (`#767`)
    - writing only the **real memory-info-derived fields** while zeroing the rest also failed at `1.0` (`#769`)
    - writing only the **real `cpu_percent` field** while zeroing the rest also failed at `1.0` (`#770`)
    - writing the full real payload through **`stats.get_obj()` raw writes** instead of the synchronized wrapper properties also failed at `1.0` (`#771`)
    - but the contemporaneous exact rerun of the original full real wrapper-property branch also failed at `1.0`, so that raw-write split is not yet a clean mechanism proof (`#772`)
    - adding a real **`mem_percent`** to the **memory-info-only** subset still failed at `1.0` (`#773`)
    - adding a real **`mem_percent`** to the **cpu-percent-only** subset softened only to `0.8333` (`#774`)
    - adding real **`mem_percent`** to the full real `memory_info + cpu_percent` write set still failed at `1.0` (`#775`)
    - keeping only the non-duplicated real **`cpu_percent + mem_rss + mem_vms + mem_shared`** fields also failed at `1.0` (`#776`)
    - keeping only the real **`cpu_percent + mem_rss_total + mem_vms_total`** fields recovered `0.5` (`#777`)
    - keeping only **`cpu_percent + mem_rss_total`** weakened to `0.6667` (`#778`)
    - keeping only **`cpu_percent + mem_vms_total`** also weakened to `0.6667` (`#779`)
    - keeping only the late total-field pair **without** a real `cpu_percent` failed at `1.0` (`#780`)
    - writing that sharpened successful late-total-fields subset through **`stats.get_obj()` raw stores** instead of wrapper properties failed at `1.0` (`#781`)
    - the contemporaneous wrapper-property control for the same subset weakened only to `0.8333` (`#782`)
    - swapping the late-pair property-store order to **`mem_vms_total` before `mem_rss_total`** weakened to `0.6667` (`#783`)
    - moving the `cpu_percent` property write **after** the late total-field pair also weakened to `0.6667` (`#784`)
    - removing the **outer batch-held `with stats.get_lock():`** while keeping the same wrapper-property field/value/order path also weakened to `0.8333` (`#785`)
    - keeping the outer batch lock and shrinking the worker to only the three ordered wrapper-property stores **`cpu_percent -> mem_rss_total -> mem_vms_total`** recovered **`0.1667`** once (`#786`)
    - but the exact rerun of that same minimal batch-held triple weakened back to `0.8333` (`#787`)
    - removing the outer batch lock from that same minimal ordered triple collapsed cleanly to `1.0` (`#788`)
    - keeping the same minimal triple under the outer batch lock but writing through **`stats.get_obj()` raw stores** softened only to `0.6667` (`#789`)
    - trying to mimic the property-setter reentrant lock churn manually around those same raw stores still collapsed to `1.0` (`#790`)
    - adding a single extra adjacent-field property write **`mem_percent=0.0`** to that minimal triple also collapsed it to `1.0` (`#791`)
    - adding a single extra far-offset trailing property write **`num_child_procs=0`** likewise collapsed it to `1.0` (`#792`)
    - even a single extra **duplicate** property write to an already-live **late** field (**`mem_vms_total`** written twice) weakened it back to `0.8333` (`#793`)
    - duplicating the **first** setter (**`cpu_percent`** written twice) also weakened it back to `0.8333` (`#794`)
    - keeping the same outer lock plus nested acquire/release churn, but doing a **fresh `stats.get_obj()` lookup inside each nested critical section**, still failed at `1.0` (`#795`)
    - while switching that same nested raw-write path from **`stats.get_obj()`** to direct private **`stats._obj`** recovered the partial `0.6667` class (`#796`)
    - directly calling the generated setter bodies via **`type(stats).<field>.fset(stats, value)`** still failed at `1.0` (`#797`)
    - even direct descriptor calls via **`type(stats).<field>.__set__(stats, value)`** also still failed at `1.0` (`#798`)
    - built-in **`setattr(stats, field, value)`** softened only to `0.8333` (`#799`)
    - direct **`object.__setattr__(stats, field, value)`** recovered the stronger partial `0.5` class once (`#800`)
    - but the exact rerun of that same **`object.__setattr__(...)`** branch collapsed cleanly back to `1.0` (`#801`)
    - replacing the outer batch hold with direct wrapper-bound **`stats.acquire(); ...; stats.release()`** also collapsed cleanly to `1.0` (`#802`)
    - replacing it with the wrapper's own context manager **`with stats:`** only softened to `0.8333` (`#803`)
    - moving the same three plain assignments into a module-level helper called inside the original **`with stats.get_lock():`** also collapsed cleanly to `1.0` (`#804`)
    - prebinding the raw lock and using **`lock = stats.get_lock(); with lock:`** recovered the stronger partial `0.6667` class once (`#805`)
    - but the exact rerun of that same prebound raw-lock context branch weakened back to `0.8333` (`#806`)
    - rebinding only the synchronized wrapper to a local alias **`s = stats`** while keeping the original inline **`with stats.get_lock():`** and the same three plain stores recovered the stronger partial `0.5` class once (`#807`)
    - but the exact rerun of that same local-wrapper-alias branch weakened back to `0.8333` (`#808`)
    - moving only the outer lock lookup onto that same local alias (**`s = stats; with s.get_lock():`**) also weakened back to `0.8333` (`#809`)
    - while the sharper **get-lock-only** alias branch — **`s = stats; with s.get_lock():`** but keeping the plain **`stats.*`** stores — recovered a reproducible `0.6667 -> 0.6667` (`#810`, `#811`)
    - prebinding only the bound **`get_lock`** method locally (**`get_lock = stats.get_lock; with get_lock():`**) also stayed in that same weaker `0.6667` class (`#812`)
    - pulling **both** late-field value computations **`int(mem_info.rss)`** and **`int(mem_info.vms)`** out of the original outer lock weakened the branch to `0.8333` (`#813`)
    - pulling only the **final** late-field computation **`int(mem_info.vms)`** out of the lock collapsed cleanly to `1.0` (`#814`)
    - pulling only the **middle** late-field computation **`int(mem_info.rss)`** out of the lock still preserved the weaker `0.6667` class (`#815`)
    - moving only the raw **`mem_info.vms`** attribute load out of the lock while keeping **`int(...)`** inside still collapsed cleanly to `1.0` (`#816`)
    - moving only the raw **`mem_info.rss`** attribute load out of the lock while keeping **`int(...)`** inside also collapsed cleanly to `1.0` (`#817`)
    - but if the pre-lock local already holds the fully computed integer **`mem_rss_total = int(mem_info.rss)`**, then even re-wrapping it as **`int(mem_rss_total)`** inside the lock still preserves the weaker `0.6667` class (`#818`)
    - by contrast, the analogous final-field variant **`mem_vms_total = int(mem_info.vms)`** outside and then **`int(mem_vms_total)`** inside still collapses cleanly to `1.0` (`#819`)
    - keeping the final-field computation **inside the same lock and same code object**, but splitting it into **two inside-lock statements** — **`mem_vms_total = int(mem_info.vms)`** followed by **`stats.mem_vms_total = mem_vms_total`** — still weakens to `0.8333` (`#820`)
    - and the middle-field counterpart **`mem_rss_total = int(mem_info.rss)`** followed by **`stats.mem_rss_total = mem_rss_total`** inside that same original lock also weakens to `0.8333` (`#821`)
    - still under direct shared memory, **clear then lock-only** with **no field reads at all** also failed cleanly at `1.0` (`#740`)
    - **lock-only without clearing the event first** weakened only to `0.8333` (`#741`)
    - **lock-then-clear** also weakened only to `0.8333` (`#742`)
    - but after **clearing** the event, even just **calling `get_lock()` without acquiring it** still failed cleanly at `1.0` (`#743`)
    - and even without clearing first, a **bare `_lock` attribute touch** on the synchronized wrapper also still failed cleanly at `1.0` (`#745`)
    - touching the synchronized wrapper's **`_obj` attribute** after **clear** also still failed cleanly at `1.0` (`#746`)
    - and that same bare **`_obj` attribute** touch **without clear** also still failed cleanly at `1.0` (`#747`)
    - calling the synchronized wrapper's public **`get_lock()` accessor** **without clear** also still failed cleanly at `1.0` (`#748`)
    - directly calling the wrapper-bound **`acquire(); release()`** path **without clear** also still failed cleanly at `1.0` (`#749`)
    - and using the wrapper's own **`with monitor._cpu_stats_smem:`** context-manager path **without clear** also still failed cleanly at `1.0` (`#750`)
    - forcing the **CPU worker** off spawn on that CPU-only route improves it reproducibly to `0.8333 -> 0.8333` (`#686`, `#687`)
    - forcing only the **manager** off spawn on that CPU-only route does **not** help (`#688`)
    - moving **both** the **manager** and the **CPU worker** off spawn on that CPU-only route hit `0.3333 -> 0.8333` (`#690`, `#691`)
    - on the broader full-monitor route:
      - **manager only** off spawn: `1.0` (`#701`)
      - **CPU worker only** off spawn: `1.0` (`#697`)
      - **GPU worker only** off spawn: `0.8333` (`#705`)
      - **CPU+GPU workers** off spawn, manager default: `0.5 -> 1.0` (`#702`, `#703`)
      - **manager+CPU** off spawn, GPU default: `0.6667 -> 1.0` (`#698`, `#699`)
      - **manager+GPU** off spawn, CPU still spawned: `1.0` (`#704`)
      - **all three** off spawn: `0.6667 -> 0.6667 -> 1.0` (`#694-#696`)
  - so the old-train-only route is **not** exhausted as a process-layer story after all, but none of the new branches is a clean recovered anchor yet. On this detached historical surface the **CPU monitor path** still looks harsher than the **GPU monitor path**, the **CPU worker's spawned bootstrap** remains the sharpest live lever, and worker-side de-spawning now looks more consistently useful than manager-only changes. The manager interaction is clearly **branch-dependent** rather than uniformly good or bad, and the now-near-complete full-route matrix says **GPU-only** off-spawn can soften the raw `1.0` class a bit, but the sharper actor is still the **CPU worker**. The newer timing probes now prune a narrower escape hatch too: this does **not** look like simple polling load, it does **not** look like a bug confined to the CPU worker spawning during the first seconds of training, and `#708` says it is **not** explained just by the spawned CPU worker talking through **manager-backed proxies** either. The newest detached CPU-only split is sharper still: the bad class is **not** explained by spawned-child presence alone (`#710`), **not** explained by transport alone (`#708`, `#712`), **not** dependent on recursive child-process scans (`#711`), and **not** driven by `proc.cpu_percent()` alone (`#713`). `#714-#750` now sharpen the parent-only `psutil` picture a lot: **`proc.memory_info()` by itself** still recovers the stronger `0.6667` class, **`proc.memory_percent()` by itself** only weakens to `0.8333`, **`cpu_percent() + memory_percent()`** also only weakens to `0.8333`, **`memory_info() + memory_percent()`** still recovers `0.6667`, and **`cpu_percent() + memory_info()`** is still the only pair that ever broke into the newer `0.5` class — but only on the original single-read `memory_info() -> cpu_percent()` branch (`#718-#720`). A one-call `cpu_percent()` warmup only weakens that branch to `0.8333`, and reversing the per-iteration order to **`cpu_percent() -> memory_info()`** collapses it cleanly to `1.0` both with and without warmup. The newer probes now split the mechanism into three separable effects. First, merely **touching** `cpu_percent()` before `memory_info()` is already enough to weaken the branch to `0.8333` even if the emitted stats are **memory-info-only** (`#726`). Second, the stronger **`1.0`** class inside the multi-call variants is now about **which `cpu_percent()` read is emitted**. If the loop emits the **second** `cpu_percent()` reading after `memory_info()`, it still recovers `0.6667` (`#725`). If the loop emits the **first** `cpu_percent()` reading for the interval — either **before** `memory_info()` (`#727`) or even as the **first post-`memory_info()`** read (`#728`) — the branch collapses cleanly to `1.0`. And `#729` shows that a **single** post-`memory_info()` `cpu_percent()` touch is **not** enough to poison the branch by itself if that value is **not emitted**. Third, the consumer-level splits now map the downstream side more tightly: the harmful path is **not** just the one visible `logger.log_scalar("percent", stats.cpu_percent, ...)` call (`#730`), it still appears when the full **read path** is kept but **all** logging is removed (`#732`), it softens when the consumer is reduced to only **event-proxy `is_set()/clear()`** with **no shared-value fetch** (`#733`), it returns to the full bad class when a raw **shared-value fetch** is added back **after** the clear (`#734`), and it weakens again both when that same raw fetch happens **without** clearing first (`#735`) and when it happens **before** the clear (`#736`).   - old train Arrow payload remains the strongest non-repo artifact clue, but it is still not a fully recovered baseline yet

- **Bad symlinked surface is still the best mechanistic path, but keep it hypothesis-driven:**
  - the broader `managerinspect` overlay is now downgraded after `#658-#659`
  - the newer temp-script canonicalization branch is also now downgraded:
    - symlinked temp-script launch + canonicalized `__main__.__file__` / `argv0` / top-level `co_filename` improved once to `0.8333` after a cache rebuild (`#662-#663`)
    - but the matching **direct temp-script control** on the **same rebuilt cache surface** fell to `1.0` (`#664`)
    - and the **symlinked rerun** also fell to `1.0` (`#665`)
  - so that temp-script branch should now be treated as another **noise-prone side clue**, not a primary live fix path
- **Cache-provenance picture remains sharp:**
  - fully local worktree + old HF + **current-format main caches**: `1.0`
  - fully local worktree + old HF + **current-format tokenized only**, rebuilt `unpadded/train`: `1.0`
  - fully local worktree + old HF + **old-format tokenized only**, rebuilt `unpadded/train`: `1.0`
  - fully local worktree + old HF + **old full old-format caches**: `0.6667 -> 0.6667`
- **Direct payload clue still stands:** under the same cache key `train/a009c9c8eee13c4a_e4146c08`, old vs current train caches differ materially:
  - row count `12365` vs `12315`
  - padded shapes `156/122` vs `155/121`
  - first-1000 row signatures had zero positional matches and zero set overlap
- **Older HF stack still matters:** dropping it on the old-train-only surface regresses cleanly to `1.0`.
- **Old `datasets` runtime package is still downgraded:** current `datasets 4.4.2` can still hit `0.6667` when the import/cache surface is otherwise right.
- **Corrected repo-surface picture:** the old-root mixed-surface effect is **not** just “current script + old `xax` works”, and it is **not** about the exact current script path, repo root on `PYTHONPATH`, split-vs-unified old root, or `task_module=examples.ljspeech` by itself.
- **What is already ruled out:**
  - old `xax` alone is not enough (`#593`, `#595`)
  - old `examples.ljspeech` namespace resolution alone is not enough (`#596`)
  - exact current main-script path is required (`#599`, `#600` falsify that)
  - current repo root on `PYTHONPATH` is required (`#601` falsifies that)
  - split old overlays are strictly required instead of the unified old root (`#603` falsifies that)
  - `task_module=examples.ljspeech` alone explains the failure (`#604` falsifies that)
  - CPU stats subprocess alone explains the failure (`#612` falsifies that)
  - GPU stats subprocess alone explains the failure (`#613` falsifies that)
  - CPU+GPU stats subprocesses together explain the failure (`#614` falsifies that)
- **What stays live:** current-script **copies** can still enter the old-regime-throughput / better-quality surface under either:
  - split old overlays (`/tmp/xaxpkg-oldexact` + `/tmp/oldexamplesonly`)
  - or the unified detached old root (`/tmp/xax-bench-9cea09c`)
  with old HF and aliased full old caches
- **Observed copied/symlinked current-script outcomes:**
  - symlinked current script + unified old root + no repo root -> `1.0` (`#595`)
  - hard-copied current script + split old overlays + repo root -> `0.8333` (`#600`)
  - hard-copied current script + split old overlays + no repo root -> `0.8333` (`#601`)
  - symlinked current script + split old overlays + no repo root -> `1.0` (`#602`)
  - hard-copied current script + unified old root + no repo root -> `0.8333` (`#603`)
  - hard copy named `/tmp/examples.ljspeech.py` on split old overlays + no repo root -> `0.8333` (`#604`)
  - symlink to the copied script (`/tmp/current-copy-link.py -> /tmp/current_ljspeech_copy.py`) on split old overlays + no repo root -> `1.0` (`#606`)
- **Most important new clues now:**
  - the broader **`disable_multiprocessing=true` rescue class is now downgraded**:
    - symlink-to-copy surface: `1.0 -> 0.6667 -> 1.0` (`#606`, `#607`, `#609`)
    - copied current-script surface: `0.8333 -> 0.5 -> 1.0` (`#601`, `#608`, `#666`)
    - direct exec-real wrapper surface: `1.0 -> 0.5 -> 1.0` (`#637`, `#638`, `#667`)
    - so removing the whole child layer can still spike, but it no longer looks like the best stable mechanistic clue
  - the narrower **no-stats** intervention is now the stronger live monitor-layer clue:
    - direct exec-real wrapper + no-stats: `0.6667 -> 0.8333` (`#640`, `#668`)
    - hard-copied current script + no-stats: `0.6667 -> 0.8333` (`#669`, `#670`)
    - trying to make that copied no-stats surface **cleaner** did **not** help:
      - unified old-root no-stats control failed at `1.0` (`#678`)
      - copied no-stats + **old-train-only** cache route also failed at `1.0` (`#679`)
    - hard-copied current script + **GPU-only** monitor: `0.8333` (`#671`)
    - hard-copied current script + **GPU-only + forked manager**: still `0.8333` (`#674`)
    - hard-copied current script + **GPU-only + forked manager + GPU worker forced off spawn**: `0.6667 -> 1.0` (`#675`, `#676`)
    - hard-copied current script + **GPU-only + GPU worker forced off spawn only**: `1.0` (`#677`)
    - hard-copied current script + **CPU-only** monitor: `0.5 -> 1.0` (`#672`, `#673`)
    - so the copied-surface monitor asymmetry still points the same way as the wrapper-side one — the **GPU monitor path** is the harsher degradant — but the copied no-stats branch now looks even more tied to the specific split-overlay + old-full-cache surface, and neither **manager spawn alone** nor **GPU-worker spawn alone** is a stable copied-surface lever
  - `multiprocessing_context=fork` is also live, but differently:
    - symlink-to-copy surface: `1.0 -> 0.8333` (`#606`, `#610`)
    - copied current-script surface: stays around `0.8333` (`#611`)
  - **no-`SyncManager`** on the bad symlink-to-copy surface is still clearly better than the raw bad-symlink `1.0` class, but no longer perfectly stable:
    - `0.6667 -> 0.6667 -> 0.8333` (`#615`, `#616`, `#618`)
  - the matching no-`SyncManager` probe on the **hard-copied** current-script surface still failed at `1.0` (`#617`)
  - **manager-only isolation:** with **both CPU and GPU stats monitors disabled** so only the broader manager/process machinery remains,
    - **spawn manager-only** stayed at `1.0` (`#614`)
    - **fork manager-only** improved to `0.6667 -> 0.8333` (`#619`, `#620`)
  - **forked manager alone is not sufficient** once spawned workers come back:
    - forked manager + default spawned workers -> `1.0` (`#621`)
  - **spawned-worker isolation on top of forked manager is asymmetric:**
    - forked manager + **CPU-only** spawned monitor worker -> `1.0` (`#622`)
    - forked manager + **GPU-only** spawned monitor worker -> `0.8333` (`#623`)
  - forcing that **CPU-only** worker to use **`fork`** improves it to `0.8333` (`#624`)
  - **all-forked children** (manager + CPU + GPU) recovered `0.6667` once, then collapsed to `1.0` (`#625`, `#626`)
  - direct bootstrap introspection of the spawned **CPU monitor worker** now shows it starts with:
    - `main_file=/tmp/current-copy-link.py`
    - `argv0=/tmp/current-copy-link.py`
    - `sys.path[0]=/tmp`
    - overlay roots immediately after that
    (`#627`)
  - direct bootstrap introspection of the spawned **manager** child now shows the same thing on the symlinked surface:
    - `main_file=/tmp/current-copy-link.py`
    - `argv0=/tmp/current-copy-link.py`
    - `sys.path[0]=/tmp`
    (`#629`)
  - and the matching hard-copied-surface probes now show the expected contrast for **both** child types:
    - manager child: `main_file=/tmp/current_ljspeech_copy.py`, `argv0=/tmp/current_ljspeech_copy.py` (`#628`)
    - CPU worker: `main_file=/tmp/current_ljspeech_copy.py`, `argv0=/tmp/current_ljspeech_copy.py` (`#630`)
  - strongest causal test so far: forcing an otherwise stronger **hard-copied** surface to present the **symlink path** `/tmp/current-copy-link.py` as `__main__.__file__` / `argv0` is enough to collapse it cleanly back to `1.0` (`#631`)
  - but the converse does **not** hold: rewriting `__main__.__file__` / `argv0` to the realpath from inside an already-symlink-launched process did **not** rescue that surface (`#632`)
  - new split: taking that same **forced-symlink-main copied surface** and removing the `ProcessMixin` child layer with `disable_multiprocessing=true` improved it to `0.8333` (`#633`)
  - but simply changing that same forced-symlink-main copied surface to `multiprocessing_context=fork` did **not** rescue it; it stayed at `1.0` (`#634`)
  - cheap minimal repro now explains part of the asymmetry: forcing `__main__.__file__` / `argv0` from inside a copied script does **not** change the parent process's already-fixed loader/code provenance — e.g. `__loader__.path` and function `co_filename` still point at the copy path even when `main_file` now says the symlink path — whereas a real symlink launch compiles the parent code objects against the symlink path from the start
  - stronger wrapper test: a symlinked wrapper that **rebound `__main__` to the copy path before any benchmark imports**, installed a real `SourceFileLoader`, and `exec`-compiled the benchmark code with `co_filename=/tmp/current_ljspeech_copy.py` still failed at `1.0` (`#635`)
  - immediate control matters: the plain hard-copied current-script surface reran right after that and still hit `0.8333` (`#636`), so `#635` is not explained away by simple copy-surface collapse/noise
  - running that same exec-real wrapper **directly** instead of via symlink still failed at `1.0` (`#637`), so the harm is not specifically about the wrapper file itself being symlinked
  - but that same direct exec-real wrapper improved sharply to `0.5` once `disable_multiprocessing=true` removed the `ProcessMixin` child layer (`#638`)
  - removing **only** the `SyncManager` from that wrapper surface did **not** reproduce the rescue; it stayed at `1.0` (`#639`)
  - disabling **both** wrapper-side monitor subprocesses recovered `0.6667` (`#640`)
  - wrapper-side **GPU monitor only** (CPU disabled) still failed at `1.0` (`#641`)
  - wrapper-side **CPU monitor only** (GPU disabled) improved only to `0.8333` (`#642`)
  - wrapper-side **GPU monitor only + forked manager** still failed at `1.0` (`#643`)
  - forcing the wrapper-side **GPU worker itself** off spawn rescued that GPU-only surface to `0.8333` (`#645`)
  - and forcing **both** the wrapper-side **manager and GPU worker** off spawn rescued further to `0.6667` (`#644`)
  - by contrast, forcing the wrapper-side **CPU worker** off spawn did **not** materially improve its weaker `0.8333` class (`#646`)
  - direct wrapper-side **GPU bootstrap instrumentation** then showed the spawned GPU worker already comes up with **`argv0=/tmp/current_ljspeech_copy.py`** and **`main_file=/tmp/current_ljspeech_copy.py`**, not the wrapper path (`#647`)
  - the complementary wrapper-side **manager-child** probe showed the same thing for the spawned manager child (`#648`)
  - the matching **true copied-script controls** then showed the same visible child payloads for both subprocess types:
    - copied-script **GPU worker** matches the wrapper-side GPU dump (`#649`)
    - copied-script **manager child** matches the wrapper-side manager dump (`#650`)
  - a new `sitecustomize.py` probe then showed the same thing one level deeper for **visible `multiprocessing.spawn` prep data**:
    - wrapper-side parent `get_preparation_data(...)` + child `prepare(...)` dumps on the GPU-only surface (`#651`)
    - matching copied-script control with effectively identical visible prep fields (`#652`)
  - an even deeper `sitecustomize.py` probe then patched **`Popen._launch`**, **`spawn.get_command_line`**, **`spawnv_passfds`**, and **`reduction.dump`**:
    - wrapper-side deeper launch metadata on the GPU-only surface (`#653`)
    - matching copied-script control with effectively identical visible launch metadata (`#654`)
  - a new deeper **manager-child inspect** overlay then sharpened the **bad symlinked surface** directly:
    - symlink-to-copy **spawn-manager-only** surface unexpectedly improved to `0.3333` while dumping `_path_to_module_name(...)`, `inspect.getfile(__main__)`, visible module set, and sampled main-function `co_filename` data (`#656`)
    - matching copied-script control on the same overlay fell back to `1.0` (`#657`)
    - the paired manager-child dumps matched on `_path_to_module_name(Path(main_file))=current_ljspeech_copy`, matched on `sys.path`, and both lacked `examples.ljspeech` in `sys.modules`; the remaining visible split was the raw `main_file` / `inspect.getfile(__main__)` path itself plus the matching `co_filename` of main-defined functions like `weighted_text_sample_bucket`
  - but that manager-inspect clue has now been sharply downgraded:
    - a minimal manager-child overlay that merely touched `weighted_text_sample_bucket.__code__.co_filename` on the bad symlinked spawn-manager-only surface still landed at `1.0` (`#658`)
    - the exact rerun of the **full** manager-inspect overlay also collapsed to `1.0` (`#659`)
    - so the easy story “any main-function code-object touch rescues the bad symlinked surface” is pruned, and the broader `#656` overlay is now better treated as a likely **noise spike / fragile side effect** than as a primary live branch
- **Interpretation:** the problem is not just “any multiprocessing,” not just manager existence alone, and not just the stats workers generically. But the live picture is now split across **two related mechanisms**, not one:
  - on the original bad **symlinked** surface, the **spawned `SyncManager`** process is materially bad, the spawned **CPU monitor worker** looks like the harsher remaining bad actor once the manager is moved off spawn, and both directly prove they bootstrap through the **symlinked entry script** as `__main__`
  - on the newer **exec-real wrapper** surface, the **GPU monitor path** still looks harsher: GPU-only still fails at `1.0`, CPU-only weakens only to `0.8333`, disabling both monitors recovers `0.6667`, forcing the GPU worker off spawn alone rescues to `0.8333`, and the strongest wrapper-side recovery comes when **both** the manager and GPU worker are off spawn
  - but the wrapper-vs-copy controls now tighten the mechanistic split a lot: on that wrapper surface, **both** child types directly inspected there — the spawned **GPU worker** and the spawned **manager child** — already come up with **`/tmp/current_ljspeech_copy.py`** as `argv0` / `main_file`, the matching true copied-script controls show the same visible child payloads, the visible `multiprocessing.spawn` prep-data dumps also match, and the deeper visible `Popen._launch` / `spawnv_passfds` metadata now match too
  - so the wrapper-specific remaining harm is now **not** well explained by the same wrong-`__main__` child-identity mechanism that explains the bad symlinked surface, and **more** consistent with some other still-hidden parent/bootstrap difference that still couples strongly to the monitor layer
  - the asymmetry still matters: **presenting** the symlinked main-path identity is enough to poison a copied surface, but trying to rewrite those fields from *inside* an already-symlink-launched process is not enough to repair it
  - after `#656-#657`, the bad-symlink side no longer looks meaningfully like a visible module-name-resolution problem: `_path_to_module_name(...)`, visible `sys.path`, and the absence of `examples.ljspeech` all already match on the paired manager-child dumps, so those scans are now largely mapped / low-priority there
  - `#633-#657` plus the minimal code-object probe now say the remaining mechanism is split across **three layers**:
    - a large **spawned-child / broader monitor-process layer** component
    - a smaller but real **parent-side pre-bootstrap compiled/loader path** component fixed before the first line of the script runs
    - and an additional **outer wrapper/bootstrap identity** component — but one that still becomes much worse when the broader `ProcessMixin` child layer is active, and on that wrapper surface the harsher remaining actor now looks like the **GPU monitor path** rather than manager creation alone
- **Immediate next step:** focus directly on the **spawned child bootstrap** mechanism, the parent-side pre-bootstrap path surface, and the new outer-wrapper identity clue:
  - explain why the bad symlinked surface is now tracking the raw **`main_file` / `inspect.getfile(__main__)` / main-function-`co_filename`** identity more strongly than any visible module-name-resolution field
  - separately explain what differs on the wrapper surface **despite** children already showing the same visible `__main__` / `sys.path` payload, the same visible `multiprocessing.spawn` prep data, and the same deeper visible `Popen._launch` metadata as the true copied-script controls
  - inspect which downstream consumers care about parent `co_filename` / `__loader__.path` vs child raw main-file identity vs outer wrapper path
  - if needed, build a cheap repro that isolates bootstrap behavior without another full 10-minute run
- **High-signal next probes:**
  - cheap off-benchmark reproducer for spawned child bootstrap comparing symlinked vs hard-copied entry scripts
  - trace **consumers** of raw `main_file`, `inspect.getfile(__main__)`, and function `co_filename` rather than spending more time merely showing those fields differ
  - inspect parent-side `co_filename` / `__loader__.path` consumers, since in-process canonicalization cannot touch them
  - inspect any consumers of the *outer* startup script path / process command line that survive the exec-real wrapper
  - explain the new **old-train-only monitor flip**: on that fully local historical surface, **CPU-only** is harsher than **GPU-only**, unlike the copied/wrapper mixed surfaces, and the split now points specifically at the **CPU worker's spawned process identity plus real `psutil` monitoring work** while **manager-only** changes remain weak / branch-dependent
  - if benchmarking continues on the detached route, prefer **consumer-level hypothesis tests** or the new **write-side direct-sharedctypes splits** over more broad no-stats/all-fork reruns, more **CPU-start-timing** sweeps, or more **manager-proxy-transport / zero-emitter** variants of the same raw CPU-only route unless they test a clearly new `psutil` consumer
  - among write-side splits, the best remaining benchmarkable question is now likely **ordered wrapper-store semantics / subset-of-real-fields / exact three-store minimality** rather than more broad synthetic whole-struct patterns: after `#768-#821`, the surviving clue is no longer "a synthetic counter branch looks alive," no longer the broader "full ordered zero-fill path" story, no longer "fewer wrapper writes alone help," and no longer primarily a **nearby cache-line** story. The strongest detached direct payload now looks like a **specific interaction**: an **outer batch-held lock** plus just the three ordered wrapper-property stores **`cpu_percent -> mem_rss_total -> mem_vms_total`** recovered **`0.1667`** once (`#786`), but the exact rerun weakened to `0.8333` (`#787`), removing that outer batch lock from the same minimal triple collapsed cleanly to `1.0` (`#788`), writing the same minimal triple through raw `stats.get_obj()` stores under the outer batch lock softened only to `0.6667` (`#789`), manually imitating the nested acquire/release churn around those same raw stores still collapsed to `1.0` (`#790`), adding even a **single extra property write** — either nearby **`mem_percent=0.0`** (`#791`) or distant **`num_child_procs=0`** (`#792`) — also collapsed cleanly to `1.0`, and even a single **duplicate** setter invocation on an already-live field weakens the branch back to `0.8333` whether that duplicate is the **last** setter (`#793`) or the **first** setter (`#794`). The newer raw/descriptor/attribute-setting/outer-lock splits then sharpened the remaining gap further: keeping the same nested acquire/release churn but doing a **fresh `stats.get_obj()` lookup inside each nested critical section** still failed at `1.0` (`#795`), while swapping only that object-access path to direct private **`stats._obj`** recovered `0.6667` (`#796`); directly calling the generated setter bodies via **`fset(...)`** (`#797`) and even the property object's **`__set__(...)`** (`#798`) both still failed at `1.0`; built-in **`setattr(...)`** improved only to `0.8333` (`#799`); direct **`object.__setattr__(...)`** recovered the stronger partial `0.5` class once (`#800`) but then collapsed back to `1.0` on exact rerun (`#801`); replacing the outer batch hold with direct wrapper-bound **`stats.acquire(); ...; stats.release()`** also collapsed cleanly to `1.0` (`#802`); replacing it with the wrapper's own **`with stats:`** context-manager path softened only to `0.8333` (`#803`); moving the same three plain assignments into a module-level helper called inside the original **`with stats.get_lock():`** also collapsed cleanly to `1.0` (`#804`); prebinding the raw lock with **`lock = stats.get_lock(); with lock:`** recovered `0.6667` once before weakening to `0.8333` on exact rerun (`#805`, `#806`); rebinding only the synchronized wrapper to a local alias **`s = stats`** while keeping the original inline **`with stats.get_lock():`** and the same three plain stores recovered `0.5` once before weakening to `0.8333` on exact rerun (`#807`, `#808`); moving only the **`get_lock()`** receiver onto a local alias while keeping the plain **`stats.*`** stores recovered a reproducible `0.6667 -> 0.6667` (`#810`, `#811`); and prebinding only the bound **`get_lock`** method locally also stayed in that same `0.6667` class (`#812`). The newest source-expression splits sharpened the lock-held write surface still further: pulling **both** late-field value computations **`int(mem_info.rss)`** and **`int(mem_info.vms)`** out of the lock weakens to `0.8333` (`#813`); pulling only the **final** late-field computation **`int(mem_info.vms)`** out of the lock collapses to `1.0` (`#814`); pulling only the **middle** late-field computation **`int(mem_info.rss)`** out of the lock still preserves `0.6667` (`#815`); moving only the raw **`mem_info.vms`** attribute load out of the lock while keeping **`int(...)`** inside still collapses to `1.0` (`#816`); moving only the raw **`mem_info.rss`** attribute load out of the lock while keeping **`int(...)`** inside also collapses to `1.0` (`#817`); once the middle-field pre-lock local already holds the fully computed integer **`mem_rss_total = int(mem_info.rss)`**, even re-wrapping it as **`int(mem_rss_total)`** inside the lock still preserves `0.6667` (`#818`); the analogous final-field variant **`mem_vms_total = int(mem_info.vms)`** outside and then **`int(mem_vms_total)`** inside still collapses to `1.0` (`#819`); and even keeping either memory-field computation **inside the same lock and same code object**, but splitting it into a local-compute line plus a following wrapper-property store line, weakens to `0.8333` for both the **final** `mem_vms_total` case (`#820`) and the **middle** `mem_rss_total` case (`#821`). So the live story is now sharper still: the only branch that ever broke into the new best class is the **exact minimal ordered synchronized-wrapper triple under the specific inline `with stats.get_lock():` hold**, the weaker partial rescue is still compatible with raw **private `_obj`** stores, the toxic ingredient in the failed manual raw paths still looks more specifically like the **`get_obj()` accessor path**, and the remaining gap is now concentrated around the true wrapper-property assignment surface plus the exact outer lock-hold surface and, now, a two-part source-expression story: **both** live memory-field stores dislike being split into a local-then-`STORE_ATTR` two-statement path inside the lock, while the **final `mem_vms_total`** path is also much less movable across the lock boundary than the **middle `mem_rss_total`** path. The **`object.__setattr__`**, prebound-raw-lock, and local-wrapper-alias paths are still useful clues about generic attribute-setting and lock-hold semantics, but all are downgraded as stable repair branches, and the broader outer-lock story is narrower than merely 'same lock held somehow' or even 'same raw lock held through a context manager somehow'
  - the cleanest deferred detached CPU-only micro-split is now narrower still: `#721-#801` say the important thing is **not** a trivial `cpu_percent()` warmup, and **not** merely whether `cpu_percent()` appears somewhere in the loop. At this point the last useful benchmarkable distinctions have mostly been mapped: a weaker harm comes from **touching `cpu_percent()` before `memory_info()`**, while the full bad class in the newer multi-call variants appears to require actually **emitting the first `cpu_percent()` reading of the interval**. `#729` further says a single post-`memory_info()` `cpu_percent()` touch is **not** enough to poison the branch if that value is **not emitted**, `#730` says the bad class is **not** explained by the one visible `logger.log_scalar` consumer alone, `#732` says it survives even when **all payload logging** is removed, `#733` says it softens when the consumer is reduced to only **event-proxy `is_set()/clear()`**, `#734` says a raw shared-value fetch added **after** that clear is already enough to restore `1.0`, `#735-#736` say the manager-proxy path weakens to `0.8333` when the fetch is moved before or independent of the clear, and `#737-#801` now show that under **direct shared memory** the route stays at `1.0` for **clear+read**, **clear+lock-only**, **clear then `get_lock()` without acquisition**, **bare wrapper-attribute touches** on both `_lock` and `_obj`, no-clear **`get_lock()`**, no-clear direct **`acquire()/release()`**, and even the wrapper's own **`with wrapper:`** path, while the only consistently live consumer branch is still **clear-only**. The write-side splits now sharpen what remains: **no writes**, **worker lock-only**, and **constant zero writes** all soften only to `0.8333`; a tiny **alternating toggle**, a **same-counter-everywhere** pattern, and a **constant per-field-distinct** pattern all fail at `1.0`; the one synthetic `0.6667` branch from `#764` failed its exact rerun (`#768`); the obvious **real subset** controls — **memory fields only** (`#769`) and **cpu-percent only** (`#770`) — also fail at `1.0`; the **memory-info + mem_percent** subset still fails at `1.0` (`#773`); **cpu-percent + mem_percent** softens only to `0.8333` (`#774`); the non-duplicated **`cpu_percent + mem_rss + mem_vms + mem_shared`** core also fails at `1.0` (`#776`); the earlier broader late-total subset with extra zero writes recovered `0.5` once and `0.8333` on contemporaneous rerun (`#777`, `#782`); either single late total field with a real `cpu_percent` weakens only to `0.6667` (`#778-#779`); the late total-field pair **without** a real `cpu_percent` fails at `1.0` (`#780`); writing the same sharpened late-pair subset through **`stats.get_obj()` raw stores** fails at `1.0` (`#781`); swapping the property-store order of the late pair weakens to `0.6667` (`#783`); moving the `cpu_percent` property write after the late pair also weakens to `0.6667` (`#784`); removing the outer batch lock from the broader ordered path weakens to `0.8333` (`#785`); shrinking the worker to only the ordered batch-held wrapper-property triple **`cpu_percent -> mem_rss_total -> mem_vms_total`** spikes to `0.1667` once and weakens to `0.8333` on exact rerun (`#786-#787`); removing the outer batch lock from that same minimal triple collapses to `1.0` (`#788`); writing that same minimal triple through raw stores under the outer batch lock softens only to `0.6667` (`#789`); manually recreating the nested acquire/release churn around those same raw stores still collapses to `1.0` when the raw path goes through **`stats.get_obj()`** (`#790`, `#795`); switching only that nested raw path to direct private **`stats._obj`** recovers the weaker `0.6667` class (`#796`); directly calling either the generated setter bodies with **`fset(...)`** (`#797`) or the property descriptors with **`__set__(...)`** (`#798`) still collapses to `1.0`; built-in **`setattr(...)`** weakens only to `0.8333` (`#799`); direct **`object.__setattr__(...)`** recovered the stronger partial `0.5` class once (`#800`) but then collapsed back to `1.0` on exact rerun (`#801`); adding even a single extra nearby or distant property write to that minimal triple also collapses to `1.0` (`#791-#792`); and even a single extra **duplicate setter invocation on an already-live field** weakens it back to `0.8333` whether the duplicate is **late** (`#793`) or **early** (`#794`). So if benchmarking continues, the best move is now to leave the broad `psutil` family and broad synthetic pattern sweeps alone and pivot to **cheap repro or code inspection** of the **event-consumption + synchronized-wrapper-touch semantics plus the writer-side sharedctypes store semantics themselves** — especially why manager proxies soften some read-order cases, why direct shared memory makes even tiny **wrapper/accessor/context-manager** operations toxic on the read side, why **`get_obj()`** is materially harsher than direct private **`_obj`** on the nested raw-write path, and what exactly differs between the true instance-attribute assignment path and the broader family of manual attribute-setting substitutes (`setattr`, `object.__setattr__`, `fset`, `__set__`). But the new evidence also says not to treat **`object.__setattr__`** as a recovered branch yet: it is a noisy clue, not a stable anchor. So the next move should still be explanation-first, not more blind reruns of attribute-setting API variants — and still not more broad CPU-monitor families
  - if still needed, compare even lower-level wrapper-vs-copy parent-only launch state that is **below** the already-mapped `get_preparation_data(...)` / `prepare(...)` / `Popen._launch` surface — but treat more visible launch-metadata scans and more visible module-name-resolution scans as largely mapped and low-priority now
  - avoid many more blind reruns of the copied no-stats cleanup branch, the broader `disable_multiprocessing=true` rescue class, the managerinspect overlays, or the temp-script canonicalization branch until a clearer consumer-level mechanism appears
- **Pruned hypothesis:** generic “deeper `xax/...` diff beyond benchmark cluster” is too broad as the immediate next step.
- **Pruned hypothesis:** simple task/module identity alone explains the drift. `#604` weakened that substantially.
- **Pruned for now:** more detached **direct-shared-memory no-clear wrapper-touch permutations** around `get_lock()` / `acquire()` / `with wrapper:` without a new code-level hypothesis. `#741`, `#748-#751` have mapped that corner tightly enough — and the last apparent softened `with get_lock()` branch collapsed on exact rerun — so the next move should be code inspection, not more tiny lock-touch variants.
- **Pruned for now:** broad new clear-only transport sweeps beyond the now-mapped corners (**manager/manager**, **direct/direct**, **direct-event+manager-value**, **manager-event+direct-value**, plus the first **direct-monitor-event + manager-start-event + direct-value** split). The live detached CPU-only branch is now more usefully framed as a **writer-side direct-sharedctypes semantics** question — **no writes** vs **lock-only** vs **constant stores** vs **synthetic whole-struct stores** vs **real psutil-derived stores / real-field subsets** — with manager-vs-direct `Event.clear()` now a lower-priority secondary question unless code inspection surfaces something new.
- **Pruned for now:** more broad synthetic whole-struct write families on the detached direct branch. After `#764-#768`, the only synthetic branch that ever helped failed its exact rerun, so more toggle/counter/pattern permutations are lower value than code inspection or sharper **real-field** / **cache-line** hypotheses.
- **Pruned for now:** more raw **`get_obj()`** caching/freshness permutations on the detached minimal-triple branch. `#789`, `#790`, `#795`, and `#796` now map that corner tightly enough to say the accessor itself is a live distinction: raw stores can still reach the weaker `0.6667` class under the outer lock, and nested manual churn can also reach that weaker class once the path uses direct private **`_obj`**, but nested raw paths that go through **`get_obj()`** stay at `1.0`.
- **Pruned for now:** more manual **descriptor-call rewrites** of the detached minimal-triple branch without a new code-level reason. After `#797-#798`, neither direct **`fset(...)`** calls nor direct property **`__set__(...)`** calls reproduce the ordinary wrapper-property assignment path.
- **Pruned for now:** more blind attribute-setting API reruns on the detached minimal-triple branch. `#799-#821` were useful to map the hierarchy — `setattr` only softens, `object.__setattr__` hit `0.5` once then collapsed, moving the same plain assignments into a helper code object collapsed to `1.0`, rebinding the wrapper object to a local alias only yielded a noisy `0.5 -> 0.8333` clue, and the newer get-lock/source-expression splits now say the more stable signal is in the outer-lock / lock-held-expression surface rather than in more store-target spelling churn — but the next move should now be explanation-first around **why** these assignment surfaces differ, not more syntax/API permutations or more blind inside-lock local-temporary rewrites without a sharper code-level consumer.
- **Pruned for now:** more blind outer-lock-hold API rewrites on the detached minimal-triple branch. `#802-#806` now say the helpful branch is narrower than merely 'same lock held somehow': direct `stats.acquire()/release()` collapses to `1.0`, `with stats:` only softens to `0.8333`, and even a prebound raw-lock context `lock = stats.get_lock(); with lock:` only reached `0.6667` once before weakening on exact rerun. Unless code inspection yields a very specific new reason, the next move should be explanation-first around why the original **inline `with stats.get_lock():`** surface is special.
- **Still-live historical path:** the fully local detached-worktree + old-train-only route remains the best explanation-first sandbox, but keep probing it hypothesis-first around the monitor/manager split rather than doing blind reruns. After `#695-#712`, the cleaner current picture is: **manager-only** changes look weak, **CPU worker** spawn remains the harsher actor, **GPU-only** off-spawn can soften the full route a bit, the newer **delay-start** failures say the detached CPU damage is **not** just an early-start timing quirk, `#708` says it is **not** just manager-proxy transport, and `#710-#712` say the harsher detached CPU route needs **real `psutil` monitoring work** more than mere spawned-child presence. The better routes are now consumer-level tracing or very targeted `psutil`-surface ablations rather than more no-stats cleanup, start-timing sweeps, or transport-only rewires.
- **Pruned for now:** already-mapped noisy model neighborhoods — sampled grouped 2-head, diff-targeted sampled grouped 2-head, mixed-text grouped 2-head, aligned normalized 2-head exact-last, grouped-4 transfers, rerankers, cheap train/infer-match stabilizers, fuller-LoRA fallback paths.
- **Pruned for now:** direct jump to `jax==0.9.2` / `jaxlib==0.9.2` / `jax-cuda12-plugin==0.9.2` as the lower-level fix. It crashes the historical code path with a sharding/vmap incompatibility.
- **Still pruned as standalone fixes:**
  - older `datasets` only
  - old HF only without the corrected import surface
  - current tokenized cache reused with older `datasets`
  - old tokenized provenance alone
  - old Qwen snapshot pinning
- **Rule of thumb:** until either the train-cache-only route stabilizes or the `SyncManager` interaction is explained, prefer experiments that improve apples-to-apples comparability over experiments that search new decoder/text-mix neighborhoods.
