- **Current best recorded result is now `1570c6b` with `asr_wer_prefix=0.3333`**, but it is still fragile. The new best came from the **tokenizer-aligned four-head grouped-prefix branch** with `text_source=both`, `semantic_future_prediction_steps=4`, `semantic_future_prediction_weight=0.5`, `semantic_self_condition_prob=0.5`, `semantic_eos_weight=2.0`, the p95-style cap, and **three-token blockwise decoding only for the first 12 generated semantic tokens**, then token-by-token decoding afterward. It also emitted EOS on both prompts and drove CER down to `0.318`, but two fresh reruns of the same recipe have now both fallen back to `0.8333`, so this is a very promising new best rather than a stable baseline yet.
- The plain four-head future-Q0 winner at `2bf02f5` (`asr_wer_prefix=0.6667`) is still the most reproducible *old* strong baseline. More importantly, the newer tokenizer-aligned four-head grouped-prefix mainline at `1d7f2f9` also tied that `0.6667` score and then held it on exact rerun, so the strongest reproducible backbone is now the **aligned four-head future-Q0 model plus a short grouped prefix**. Nearby scalar retunes around the old exact-decoding four-head baseline remain stale.
- A new structural clue is the **tokenizer/model-vocab alignment fix**: Qwen's config vocab is padded above the live tokenizer length, so newly added audio tokens were previously landing partly inside the frozen base-vocab range instead of entirely in the dedicated extra-token path. Padding the tokenizer up to the model vocab before adding text/audio/Q0 tokens improved the four-head future-Q0 baseline from the current `1.0` baseline to `0.8333` and, more importantly, finally made EOS fire on both prompts with much shorter generations (~41/39 frames). Its first transfer to the original grouped-prefix winner also tied at `0.8333` with EOS on both prompts and stronger in-domain WER `0.556`, while transferring the old grouped-prefix decode policy onto the aligned four-head baseline (`semantic_block_decode_size=3`, first 12 semantic tokens grouped) tied the historical robust best at `0.6667` and then held that score on exact rerun with even better CER `0.545`. A further transfer of the old mixed-text clue onto that aligned grouped mainline produced the new `0.3333` best before collapsing on two reruns to `0.8333`, while the fuller LoRA target-set transfer on that same mixed-text aligned grouped branch has now held `0.6667` twice as a more reproducible fallback. A newer weighted-text family also now has real life: the first 2:1 normalized/raw mix (`text_source=both_plus_normalized`) reached `0.5` once with very strong CER `0.409` and much higher 10-minute throughput before collapsing to `1.0` on exact rerun, while a later generic 3:1 normalized/raw mix (`text_source=both_weighted`, repeats 3/1) also reached `0.5` and then softened only to `0.8333` on exact rerun. Pushing farther toward normalized text at 4:1 only returned `0.6667`, and the opposite raw-heavy 2:1 mix failed cleanly at `1.0`. The aligned exact-last grouped transfer still failed at `1.0`, and the same remained true even after adding mixed-text conditioning. So the alignment fix now looks like a real new foundation, and the strongest live path is the **aligned four-head grouped-prefix branch**, with mixed-text conditioning newly reactivated on top of it.
- The hybrid grouped-prefix branch remains the clear place where the biggest wins happen, but it also still looks like a **high-variance, tightly localized effect**. The old matched two-head grouped recipe at `cf0f36a` (`0.5`) already failed two exact reruns, and the new aligned four-head mixed-text grouped best at `1570c6b` (`0.3333`) has now fallen back to `0.8333` on two separate fresh reruns. So grouped-prefix decoding is unquestionably important, but the exact sweet spots are still fragile and need stabilization rather than blind local retuning.
- The local neighborhood around the grouped-prefix transition is now pruned fairly aggressively:
  - hard cutoffs at **9** or **15** grouped tokens regressed to `1.0`
  - an **exact partial final grouped block** helped somewhat at **11** tokens (`asr_wer_prefix=0.8333`) but did not beat the best
  - a longer partial schedule at **14** tokens recovered good secondary metrics (`asr_cer=0.636`, in-domain WER `0.556`) but still failed on the primary metric
  - a simple **3-token early groups -> 2-token late groups** taper also reached only `0.8333`
  - **corrective overlap schedules** that re-decoded the last **1** or **2** grouped-prefix tokens exactly were too slow and failed badly (`asr_wer_prefix=1.0` in both cases)
  - **no-second-pass verification schedules** were also disappointing: verifying every grouped extra token reached only `0.8333`, verifying only the last grouped extra token fell back to `1.0`, and verifying only the final transition token also stayed at `1.0`
  - a more aggressive early-grouped-prefix variant using **4-token groups** with **3** future-Q0 heads in training also fell back to `1.0`
  - a new intra-block hybrid transition that made the **middle token exact** and used a future head only for the **third** token of each 3-token grouped step also fell back to `1.0` and slowed throughput
  - a different intra-block hybrid transition that ends each grouped step on an **exact last token** is the first genuinely new transition design since `cf0f36a` to show strong life: its first 12-token grouped-prefix run reached `asr_wer_prefix=0.6667` and matched the best CER seen so far at `0.591`, but its neighborhood is now clearly narrow and high-variance — shortening the grouped prefix to **6** or **9** or lengthening it to **15** all regressed to `1.0`, and an exact rerun of the nominal 12-token setup also fell back to `1.0`
  - a more explicit **transition-only exact-last schedule** also now looks mostly stale: applying the exact-last-token grouped step only on the final **1** grouped step before fallback kept decent in-domain prefix WER (`0.333`) but still failed on the primary metric and was too slow, and expanding that schedule to the final **2** grouped steps also stayed at `1.0`
  - an **early-only exact-last schedule** also failed: using exact-last grouped steps for the first **9** grouped tokens and then returning to the plain two-head grouped path for the final grouped step before fallback still regressed to `1.0`
  Together these suggest that boundary smoothing helps a bit in some forms, but the simple fixed-cutoff, fixed-overlap, fixed verification, simple 3->2 taper, larger early-group variants, and the exact-middle / future-last hybrid are now mostly stale. A more explicit **head-position-matched 3->2 taper** also failed: masking the later future head to only the first 6 grouped / Q0 positions in both inference and training still returned `1.0`. So the newer **exact-last-token grouped transition** remains only a fragile, tightly localized clue.
- Several nearby **training-side train/infer-match ideas** also now look mostly stale:
  - increasing the matched grouped-prefix branch from **2** to **3** future-Q0 auxiliary heads in training reached only `0.8333`
  - restricting the two-head future-Q0 auxiliary only to the first **12 early semantic target positions** regressed to `1.0`
  - restricting the auxiliary to **early anchor positions spaced every 3 Q0 tokens** (to mimic the grouped decode steps) also regressed to `1.0`
  - an even stricter grouped-step-matched future-head supervision mask that only keeps source positions and head offsets actually consumed by the fixed grouped decode policy (`semantic_future_prediction_match_block_decode=true`) also failed on both the revived broader two-head exact/future/future/exact branch and the older 3-token exact/future/exact branch, each falling back to `1.0`; a later transfer onto the aligned mixed-text four-head grouped mainline also failed twice at `1.0`, first with decent CER `0.727` and then on rerun with OOD EOS disappearing and the utterance stretching back to the 53-frame cap
  - a new **soft early-position weighting** variant for the future-Q0 auxiliary is the first training-side idea in this family to show any life after those failures: decaying future-loss weights across semantic positions on the matched grouped-prefix branch reached `asr_wer_prefix=0.8333` at `semantic_future_prediction_position_decay=0.97`, which is better than the recent `1.0` collapses but still below the strong baselines
  - that same soft weighting is also clearly fragile: a milder near-uniform decay `0.99` regressed badly to `1.0`, and applying the `0.97` decay to the plain four-head future-Q0 baseline also stayed at `1.0`
  - a new **per-head weighting** variant for the two grouped future heads also now looks stale in both directions: emphasizing the nearer grouped token (`semantic_future_prediction_head_decay=0.7`) stayed at `1.0`, and emphasizing the farther grouped token (`1.5`) also stayed at `1.0` while exploding CER to `8.0`
  So simple masking or deepening of the future auxiliary is stale, **soft early-position weighting is at most a narrow grouped-prefix-specific clue rather than a broadly good new objective**, and simple per-head reweighting of the two grouped future heads also looks exhausted.
- The **smaller matched grouped branch** now also looks weaker as a hybrid-prefix path. Training one future-Q0 head and decoding two semantic tokens at a time for only the first **8** generated tokens (four grouped steps, analogous to the four grouped steps in the winning 12-token/3-block recipe) still returned `asr_wer_prefix=1.0`. A more tightly matched one-head version of the newer **exact-last-token** transition also collapsed to `1.0`, and transferring the exact-last-token grouped decode to the plain reproducible **four-head** future-Q0 baseline also failed cleanly at `1.0`. So the current grouped-prefix clue appears specific to the stronger **two-head / three-token** matched branch.
- **Plain 1:1 mixed text still looks grouped-prefix-specific, but normalized-heavy weighted text is now a broader — still fragile — clue.** Ablating `text_source=both` from the old matched grouped-prefix recipe had already dropped that branch to `asr_wer_prefix=0.8333`, and a plain transfer of `text_source=both` onto the aligned four-head exact-decoding baseline still failed cleanly at `1.0`. But transferring `text_source=both` onto the new aligned four-head grouped-prefix mainline produced the new best `asr_wer_prefix=0.3333` with CER `0.318` before falling back to `0.8333` on exact rerun, so the original 1:1 mixed-text spike still looks tied to the grouped-prefix branch. A newer weighted family that leans **more normalized than raw** is the first alternative formulation in this neighborhood with repeatable life: the initial hardcoded 2:1 normalized/raw mix (`text_source=both_plus_normalized`) hit `0.5` with strong CER `0.409` and much higher throughput (~30.4k samples / 10 min), but its exact rerun still collapsed to `1.0`; a later generic 2:1 run on the aligned grouped branch landed only at `0.8333`, so the broader 2:1 family now looks weaker and more collapse-prone than the newer 3:1 branch. Generalizing that idea into `text_source=both_weighted` showed that a milder 3:1 normalized/raw mix can span a wider range on the aligned grouped backbone: across **eleven** grouped runs it has now produced **two `0.5` hits, one `0.6667`, five `0.8333`s, and three full `1.0` collapses**. So the branch is definitely still fragile and now clearly bimodal/noisy rather than simply softening toward `0.8333`, but it still looks more reproducible — and more promising overall — than the neighboring 2:1, 3:2, 4:1, 5:2, balanced, sampled, and exact-decoding weighted variants. A newer rerun also sharpened the failure picture: one of the fresh `1.0` results was a true long-form OOD collapse with missing EOS and the utterance stretched back to the 53-frame cap, while another still emitted EOS on both prompts and stayed short (~28/34 frames) but missed the requested content word-for-word. So the weighted 3:1 branch now seems to have at least **two distinct failure modes**, not just a single EOS/length problem. The surrounding grouped-prefix neighborhood is now narrower too: shrinking the grouped window to **9** or **3** tokens collapsed cleanly to `1.0`, extending it to **15** also collapsed to `1.0`, while **6** tokens only reached `0.8333` more slowly, so the live weighted branch currently looks tightly centered on the original **12-token** grouped prefix rather than on shorter or longer windows. By contrast, a softer 3:2 mix only reached `0.8333`, a more central 5:2 mix regressed all the way back to `1.0` despite perfect in-domain WER, and pushing farther toward normalized text at 4:1 first returned `0.6667` but then collapsed to `1.0` on exact rerun, suggesting the useful grouped-branch region is narrow and not monotonic even inside the nominal 2:1–3:1 neighborhood. The opposite raw-heavy 2:1 mix failed cleanly at `1.0`, and stacking the normalized-heavy weighted branch with the reproducible fuller-LoRA fallback failed twice (the old hardcoded 2:1 mix only reached `0.8333`, while the newer 3:1 generic mix collapsed to `1.0`). A different attempt to keep the same 3:1 normalized/raw weighting while restoring just one text variant per utterance (`text_source=both_weighted_balanced`) also failed cleanly at `1.0`, so the live weighted clue does not seem to come from duplication-free one-variant balancing either. The first per-pass weighted one-variant resampling formulation also split in an interesting way: keeping the full four sampled passes at the same overall 3:1 mass (`text_source=both_weighted_sampled` with the default sampled-pass count) failed cleanly at `1.0`, while a newer lower-mass variant that **decouples sampling probability from total sampled passes** by keeping the same 3:1 weights while building only **two** deterministic sampled passes (`text_source_weighted_sampled_passes=2`) recovered `0.5`, then `0.6667`, then `0.5` again, before finally collapsing to `1.0`, all with EOS on both prompts and full throughput. So this new sampled branch is definitely real, but it is not yet a reliably sub-1.0 backbone. Mapping the immediate neighbors made the picture sharper: **one** sampled pass also failed cleanly at `1.0`, and **three** sampled passes failed cleanly at `1.0` too. Ratio probes inside that same 2-pass family also came back narrow: lowering the weighting to **2:1** normalized/raw failed at `1.0`, and pushing harder to **4:1** also failed at `1.0`. Further seed-sensitivity probes made the story narrower still: changing only the deterministic normalized/raw assignment salt for the same live **2-pass, 3:1** sampled recipe caused catastrophic `1.0` OOD failures on multiple alternate tried seeds, even while the in-domain prompt sometimes transcribed perfectly. A different low-mass anchored formulation that kept two total passes by combining **one full normalized pass plus one balanced normalized/raw pass** for the same overall **3:1** mass also failed cleanly at `1.0`, so the sampled clue is not explained by simply forcing every utterance to keep one normalized anchor while using raw text only as a sparse second-pass perturbation. So the weighted clue is not just about fixed duplicate copies versus one-variant resampling; it now looks like the **total sampled mass within the 10-minute budget** matters, and even inside the live sampled recipe the exact utterance/pass assignment may matter too. The currently live sampled family therefore appears tightly localized around the new **2-pass, 3:1, seed-0** setting rather than broadly spread across nearby sampled masses, nearby normalized/raw ratios, or arbitrary sampled assignments. A new update broadened the story further: the older fixed-duplicate 3:1 weighted mix tied the old robust `0.6667` score once on the aligned four-head **exact-decoding** backbone, but two later independent reruns both collapsed to `1.0`, while the same exact-decoding transfer at 2:1 and 4:1 also failed cleanly at `1.0`. The new **2-pass sampled** 3:1 branch also failed cleanly when transferred to exact decoding, even while producing extremely strong in-domain WER. So the sampled stochastic-mass clue currently looks **grouped-prefix-specific**, whereas the older exact-decoding weighted life now reads as a separate, mostly stale/noisy side effect rather than as evidence for a broad exact-decoding weighted family. Nearby low-code stabilizers on top of the live grouped 3:1 branch also mostly look stale: a simple text-prefix LM anchor (`text_loss_weight=0.1`) first held only `0.8333` while improving CER to `0.636`, but it cut sample coverage down to ~23.0k / 10 min and regressed in-domain WER to `1.0`; an exact rerun of that same `0.1` anchor then collapsed back to `1.0`, and a milder `text_loss_weight=0.05` failed even harder at `1.0` while collapsing throughput to only ~13.7k samples / 10 min. But a later transfer of that same `text_loss_weight=0.1` anchor onto the narrow **2-pass sampled 3:1 grouped-prefix** branch unexpectedly tied the overall best at `0.3333` with CER `0.318`, EOS on both prompts, and short generations, even though throughput again dropped to only ~23.0k samples / 10 min and in-domain WER stayed mediocre. So the text-loss anchor still looks stale on the older fixed-duplicate weighted branch, but it may now be a real — if fragile — sampled-branch-specific stabilizer. Earlier attempts to stabilize the 1:1 mixed-text branch with explicit normalized-vs-raw marker tokens also failed in both obvious eval forms: using learned `<|text_normalized|>` / `<|text_raw|>` markers with a raw eval marker reached only `0.8333`, while the normalized eval marker regressed to `1.0`. A different attempt to keep both transcript styles without doubling the dataset size (`text_source=both_balanced`, alternating normalized/raw by utterance index) also failed cleanly at `1.0` both on the plain aligned grouped mainline and when paired with the reproducible fuller-LoRA fallback. So plain mixed-text conditioning still looks like a grouped-prefix clue, while the most promising weighted direction now looks like **3:1 normalized/raw** as a broader but unstable text-conditioning variant rather than a stabilized replacement.
- **Scheduled sampling is still essential.** Ablating `semantic_self_condition_prob=0.5` from the strong structural recipes regressed to `1.0`, and lowering self-conditioning on the matched grouped branch also regressed. So neither the future-Q0 auxiliary nor grouped decoding replaces exposure-bias training by itself.
- The strongest remaining question is now very specific: **how should the decoder transition from a short grouped prefix back to exact token-by-token decoding?** The next worthwhile variants should be more structural than the ones already tried.
- Promising structural variants to revisit later:
  - the new **exact-last-token grouped transition** is still the strongest *new* transition clue after the original `cf0f36a` winner: during each early 3-token grouped step, decode token 1 exactly, predict token 2 with a future head, then decode token 3 exactly after feeding that speculative middle token. But it is now clearly fragile: the first 12-token run reached `asr_wer_prefix=0.6667` with `asr_cer=0.591`, while nearby 9/15-token cutoffs and an exact rerun all regressed. After reintroducing the exact-last path on top of the newer schedule-capable decoder, the plain 12-token cutoff recovered only weak life (`0.8333`), exact-last **partial-final-block** variants at effective 11 (`3,3,3,2`) and 14 (`3,3,3,3,2`) grouped tokens both failed at `1.0`, fuller LoRA on the same branch only reached `0.8333`, and even lowering `batch_size` to buy many more optimizer steps still fell back to `1.0`. A later transfer of exact-last onto the new mixed-text aligned four-head grouped winner also failed cleanly at `1.0`, so mixed-text conditioning does not seem to rescue this transition on the aligned backbone either. A newly unlocked broader exact-last family mostly failed when tried by itself: allowing exact-last grouped steps to emit one more token than the number of future heads made it possible to test a true two-head exact/future/future/exact branch (`block_decode_size=4`) and even a three-head exact/future/future/future/exact branch (`block_decode_size=5` with 3 future heads), but the original tested 8/12-token and 10-token windows all stayed at `1.0`. However, the newer future-slots-only cheap targeted early-prefix training objective did finally bring this broader two-head `block_decode_size=4` path to life: one run tied the robust best at `0.6667`, an exact rerun landed at `0.8333` with strong CER `0.636`, but a later fresh exact rerun fell all the way back to `1.0` with gt-ASR sanity degradation, while both shorter 8-token and longer 16-token grouped windows also fell back to `1.0`, stacking it with the grouped-style reranker failed, a stricter grouped-step-matched future-head supervision mask regressed to `1.0`, and broader late-boundary schedule transfers like `4,4,1,3` and `4,4,3,1` also failed. So the broader two-head exact-last family is still stale by itself and now looks even more localized/noisy with the matched training objective, rather than like a robust successor once training is matched to the speculative future-token slots. The same matched-training idea also partially revived the true three-head broader exact-last path (`block_decode_size=5`, `semantic_future_prediction_steps=3`): the 15-token window and a shorter 10-token retune both reached only `0.8333`, but an exact rerun of the 15-token branch fell back to `1.0` despite strong CER `0.636`, and a late-boundary transfer `semantic_block_decode_schedule=5,5,1,4` with exact-last also failed cleanly at `1.0`. So that broader three-head family is now only a noisy side clue and clearly weaker than the two-head `block_decode_size=4` branch. A new heavy-eval self-NLL reranker that compares grouped transition styles showed one tie-level result (`0.6667`) with the best CER seen in this family (`0.545`), but its exact rerun already collapsed to `1.0`, the larger-LM transfer failed, broadening the candidate set to include the late-boundary schedule `3,3,3,1,2` also failed, and even adding a tokenwise-exact fallback candidate via `semantic_eval_compare_schedule_candidate=1` still returned `1.0`.  A newer heavy-eval post-hoc grouped-token refinement candidate that replaces only speculative grouped slots with exact next-token predictions after generation failed on the broader `block_decode_size=4` exact-last branch and also on both the original plain grouped 12-token mainline and the narrower 3-token exact-last grouped branch. So the live clues now center on the old plain 3-token / 12-token transition plus a newer, still-noisy matched-training version of the broader two-head exact/future/future/exact path rather than on broader exact-last local retunes alone
  - a grouped-prefix decoder with a **corrective / overlapping last token that does not require a full second exact decode pass**, but in a richer form than the now-tried naive verification schedules; a simple periodic exact-bridge token after every grouped step is now stale at both 12 and 16 grouped-token cutoffs, and a first cheap one-token boundary-overlap implementation on the aligned mixed-text grouped mainline also failed cleanly at `1.0`, so only substantially richer corrective-overlap designs remain interesting here
  - a more explicit **position-dependent transition policy**, but now in a much narrower form: the per-step decode-schedule path suggests the only surviving clue is a **late exact bridge plus one short final grouped step** near the old 12-token boundary. The first mid-prefix bridge+taper schedule (`3,3,1,2,2,1`) failed cleanly; a later boundary-focused schedule (`3,3,3,1,2`) first showed weak life at `asr_wer_prefix=0.8333` but then failed its exact rerun; removing the explicit bridge (`3,3,3,2,1`) also failed; shifting the taper one token earlier (`3,3,2,1,2`) still missed the OOD primary metric even while reaching very strong in-domain WER (`0.111`); and combining these explicit late-boundary schedules with the reintroduced **exact-last-token** grouped transition did not beat the plain reintroduced exact-last 12-token cutoff. A new schedule-aware cheap targeted early-prefix matching path did recover weak life again on the plain grouped `3,3,3,1,2` schedule when it replaced only the speculative schedule future-token slots, but its exact rerun fell back to `1.0`, while the earlier-taper `3,3,2,1,2`, the exact-last `3,3,3,1,2` version, and a whole-window replacement ablation all failed. A new heavy-eval post-hoc grouped-token refinement candidate that replaces only speculative schedule slots with exact next-token predictions also reached only weak life (`0.8333`) on that same plain `3,3,3,1,2` schedule before falling back to `1.0` on exact rerun, though the rerun did recover good in-domain WER `0.444`. New post-alignment transfers of that same `3,3,3,1,2` schedule onto both the aligned four-head branch and the aligned original grouped family also reached only `0.8333` while preserving EOS, and trying it as a stabilizer on the new mixed-text aligned grouped winner regressed all the way to `1.0`. So even after the alignment fix it still looks more like a secondary-signal clue than a robust mainline. If this family is revisited, focus on **late-boundary** schedules only as a side clue rather than as the main next baseline.
  - a training target that better matches this **early grouped, late exact** inference pattern in a richer way than the now-tried simple masks. A new future-head-based local corruption objective first showed weak life (`asr_wer_prefix=0.8333`) in an expensive double-forward form that was too slow. A cheaper piggyback version that injects the same two-positions-earlier future-head corruption inside scheduled sampling kept throughput much healthier and produced some strong secondary signals (for example exact-last in-domain WER `0.111` and grouped-mainline CER `0.682`), but it still did not improve the primary OOD metric. Two more targeted cheap variants also failed: (1) corrupting only the speculative middle-token slots of each early exact-last 3-token block and (2) a richer grouped-pattern corruption on the plain grouped-prefix mainline that used future head 0 on ranks 2/5/8/... and future head 1 on ranks 3/6/9/.... A more literal free-running grouped self-conditioning variant that actually replaced the random suffix with a blockwise decode matching inference was also a dead end in its current form: the full-suffix version collapsed throughput to only 49 steps / 392 samples in 10 minutes, a cheaper hybrid that decoded only the remaining grouped-prefix window before falling back to the usual cheap tail predictions still managed only 104 steps / 832 samples, even a sparse mixed variant that used the expensive grouped-prefix hybrid on only 10% of self-conditioned examples still managed only 109 steps / 872 samples, and a newly added targeted variant that replaced only the first grouped-prefix window from the pure text prompt still managed only 113 steps / 904 samples. Extending that targeted path so most examples fall back to the usual cheap one-step replacement still remained catastrophic at only 106 steps / 848 samples. A new fully cheap targeted early-prefix fallback did recover normal throughput by replacing only the first grouped-prefix window with teacher-forced first-pass Q0 predictions: on the plain grouped mainline it still returned `1.0`, while on the exact-last grouped branch it recovered only weak life (`0.8333`) before regressing again when saturated harder or stacked with a text anchor. A more exact-last-matched cheap variant that replaces only the speculative grouped future-token slots inside that early window also only showed weak life on the exact-last branch (`0.8333`, but with better CER `0.773`) and then failed when saturated harder, on the plain grouped mainline, on the larger-LM transfer, and with the grouped-style reranker stack. Extending that cheap targeted replacement to explicit per-step schedules recovered another weak clue on the plain late-boundary `3,3,3,1,2` schedule only when replacement was restricted to speculative schedule future-token slots, but its exact rerun already failed, and the earlier-taper schedule plus exact-last and whole-window variants all regressed. A later transfer of this future-slots-only cheap early-prefix matching idea onto the new mixed-text aligned four-head grouped winner also failed at `1.0` while only improving in-domain WER to `0.111`, so it currently still looks like another in-domain-only side effect rather than a path to stabilize the OOD win. A new direct cheap replacement that fills speculative slots from the auxiliary future-Q0 heads themselves instead of the one-step head also failed on both the broader exact-last `block_decode_size=4` clue and the schedule-aware `3,3,3,1,2` branch. So this idea is only worth revisiting if it becomes substantially more structural than the now-tried mask patterns **and** avoids the catastrophic per-example decode cost.
  - if pivoting away from grouped-prefix work, a **stronger semi-parallel Stage-1.5 refiner trained on more realistic generated/free-running Q0 errors** could still be worth revisiting later. A second prototype that trained on the actual Q0 stream seen by Stage 1 (including self-conditioned suffixes) was better matched than the first teacher-forced version and did recover some secondary signals (for example grouped-prefix in-domain WER 0.444 at refiner loss weight 0.5), but it still failed on the primary metric and did not survive a simple weight retune. A newer generated-context refiner that used a full second LLM pass over the model's own greedy Q0 sequence while keeping the text prefix fixed also failed at both `semantic_q0_refiner_loss_weight=0.5` and `0.25`, while cutting throughput to only ~10.9k samples / 10 min. So any future Stage-1.5 revisit probably needs a substantially cheaper or more targeted generated-error distribution than these first full-second-pass variants.
  - the new **weighted-text stochastic-mass** branch is now live but already looks tightly localized: the full **4-pass** sampled formulation failed, the new **2-pass** sampled setting recovered `0.5`, then `0.6667`, then `0.5`, and then collapsed to `1.0`, and both neighboring lower-mass probes (**1** pass and **3** passes) failed cleanly at `1.0`. Nearby ratio probes inside that same 2-pass family also failed in both directions: **2:1** and **4:1** normalized/raw each regressed to `1.0`. A transfer of that same 2-pass sampled 3:1 branch back to exact decoding also failed cleanly at `1.0`, and multiple alternate deterministic sampled-assignment seeds also caused catastrophic `1.0` OOD failures. So the next worthwhile question in this neighborhood is no longer whether a broad lower-mass sampled family exists; it is whether the current **2-pass, 3:1, seed-0 grouped-prefix** branch is reproducible enough to matter and whether any cheap stabilizer exists *inside that very narrow setting*.
  - a small **text-prefix LM preservation loss** is now a weak side clue rather than a mainline direction: on the reproducible four-head future-Q0 baseline, `text_loss_weight=0.1` reached `asr_wer_prefix=0.8333` while preserving gt ASR, but it also cut throughput substantially, a smaller retune to `0.02` regressed back to `1.0`, and an intermediate retune to `0.05` failed even harder with exploding transcript WER/CER. It also failed to rescue the plain grouped-prefix mainline, and it did not stack with the noisy fuller LoRA-target clue on the four-head baseline. A transfer to the 600M two-head exact-last grouped branch recovered only weak life at `0.8333` with `text_loss_weight=0.1`, then fell back to `1.0` at `0.05`, the `0.1` version also failed when combined with the grouped-style heavy-eval reranker, and it failed again when stacked with the newer cheap targeted early-prefix exact-last training objective. A later transfer onto the aligned mixed-text four-head grouped winner also failed cleanly at `1.0` while dropping throughput to only ~13.8k samples in 10 minutes. So this is only worth revisiting if the text-conditioning objective is integrated more carefully than a simple auxiliary loss weight.
  - a larger **stage-1 base LM** is now only a noisy side clue rather than a broad new pivot. Swapping from `QWEN3_600M` to `QWEN3_1_7B` on the plain four-head future-Q0 baseline failed cleanly at `1.0`, the original grouped-prefix mainline also stayed at `1.0`, and even the old frozen non-AR Stage-1 upsampler stayed at `1.0` when given the larger LM. The only live signal so far was on the larger-LM exact-last grouped branch (`text_source=both`, 2 future heads, 12-token exact-last prefix, `batch_size=4`), which tied the robust best once at `0.6667` with `asr_cer=0.636`, but an exact rerun already collapsed to `1.0`, the normalized-text ablation failed badly, the late-boundary exact-last schedule `3,3,3,1,2` also failed, fuller LoRA on the larger-LM plain baseline failed, fuller LoRA on the larger-LM exact-last grouped branch also failed, the newer cheap targeted early-prefix exact-last training objectives also failed to revive it, even transferring that cheap future-slots-only objective to the broader larger-LM `block_decode_size=4` exact/future/future/exact branch still returned `1.0`, and a fresh post-alignment transfer onto the new mixed-text aligned four-head grouped mainline also ran cleanly only to fall back to `1.0`. So treat the larger-LM path as a stale/noisy branch-specific clue, not a robust baseline upgrade.
  - a fuller **LoRA target set** on the reproducible four-head future-Q0 baseline is the first LoRA-structure change to show real life: adding the MLP down projection to the default target set (`lora_targets=[q_proj,k_proj,v_proj,o_proj,gate,up,down]`) initially tied the robust best at `asr_wer_prefix=0.6667` and matched the best CER on that baseline family at `0.591`. But its neighborhood is now clearly narrow and mostly bad: an exact rerun already fell back to `1.0`, mixed-text conditioning on the aligned **exact-decoding** four-head baseline failed, smaller rank/alpha (`8/8`), lower alpha with full rank (`alpha=8`), larger alpha (`32`), a more surgical attention+down subset (`[q_proj,k_proj,v_proj,o_proj,down]`), an MLP-only subset (`[gate,up,down]`), direct transfer to the original grouped-prefix mainline, transfer to the old four-head grouped-prefix exact-last decoder, transfer to the stronger two-head exact-last grouped branch (only `0.8333`), stacking with `text_loss_weight=0.1`, stacking with the grouped-style heavy-eval reranker on that exact-last branch, and transfer to the larger `QWEN3_1_7B` plain baseline all failed to produce a new win. A later transfer onto the new **mixed-text aligned four-head grouped** mainline first recovered `0.6667` with strong CER `0.409`, EOS on both prompts, and short generations, and an exact rerun then held the same `0.6667` again with CER `0.591`, perfect gt-ASR sanity, and very strong in-domain WER `0.111`. But trying to use that fuller target set to rescue the older aligned mixed-text **two-head** grouped family still only reached `0.8333` with in-domain EOS failure and the in-domain utterance back at the 53-frame cap. So if this line is revisited, treat the **plain fuller target set at the original alpha/rank on the aligned four-head grouped branch** as the only live LoRA fallback, not as a generic grouped-branch stabilizer.
  - a more structural follow-up to that reproducible fuller-LoRA fallback could still be a **much cheaper keyed LoRA-dropout implementation**, but the first attempt is now pruned in its current form. Wiring PRNG keys through `apply_linear` / `_apply_linear_batched`, transformer/LLM forward passes, and the stage-1 training calls made `lora_dropout` runnable, but on the mixed-text aligned grouped fuller-LoRA branch it was disastrous: `lora_dropout=0.05` collapsed throughput to only **436 steps / 3.5k samples** and regressed to `0.8333`, while even the no-dropout control on that same keyed path (`lora_dropout=0.0`) still dropped to **2216 steps / 17.7k samples** and regressed all the way to `1.0`. So the current keyed implementation should be treated as stale/bad and only revisited if the randomness can be threaded much more cheaply than this first end-to-end path.
- Treat these as mostly stale now:
  - grouped decoding for the whole utterance
  - generic grouped-branch scalar retunes (future weight `0.6` / `0.75`, shorter or longer caps, lower self-conditioning)
  - simple warmup-before-grouped schedules
  - partial-final-block schedules by themselves
  - simple 3->2 late tapering by itself
  - simple explicit bridge schedules by themselves, especially an early bridge+taper (`3,3,1,2,2,1`), a late bridge without a final short grouped step (`3,3,3,1,1,1`), or a late short grouped step without the explicit bridge (`3,3,3,2,1`)
  - simple exact-last-token + explicit late-boundary schedule combos by themselves (for example `3,3,2,1,2` or `3,3,3,1,2` with `semantic_block_decode_exact_last_token=true`), and now also the exact-last transfer on top of the new aligned four-head grouped-prefix mainline
  - simple heavy-eval self-NLL reranking between grouped transition styles by itself, including plain grouped vs exact-last grouped, the later three-way variants that also compared the late-boundary schedules `3,3,3,1,2` and `3,3,2,1,2`, and a tokenwise-exact fallback candidate via `semantic_eval_compare_schedule_candidate=1`; it produced one `0.6667` tie with very strong CER but immediately fell back to `1.0` on exact rerun, failed on the larger-LM branch, failed again after broadening the candidate set, and also failed when stacked with the newer cheap targeted early-prefix exact-last training objective
  - the new heavy-eval post-hoc grouped-token refinement candidate (`semantic_eval_compare_refined_candidate=true`) by itself on the broader `block_decode_size=4` exact-last branch, on the original plain grouped 12-token mainline, on the narrower 3-token exact-last grouped branch, and its noisy transfer to the plain late-boundary schedule `3,3,3,1,2`
  - a simple head-position-matched 3->2 taper (mask the later future head to an early prefix in both inference and training) by itself
  - naive exact overlap re-decoding schedules by themselves
  - the first cheap one-token grouped->exact boundary-overlap implementation by itself (`semantic_block_decode_overlap_last_boundary_token=true`) on the aligned mixed-text grouped mainline; it preserved EOS and short generations but still regressed cleanly to `1.0`
  - naive grouped-token verification schedules by themselves
  - simple per-token confidence gating of grouped future tokens by itself
  - the exact-middle / future-last grouped-step hybrid by itself
  - naive shorter/longer cutoff retunes of the newer exact-last-token grouped transition by themselves (now including **6**, **9**, **15**, and the newer effective **11** / **14** partial-final-block variants), and now also shorter 9-token and longer 15-token grouped-prefix retunes on the new aligned four-head grouped family / mixed-text transfer branch
  - broader plain grouped steps that try to consume more or all future heads directly at inference on the aligned four-head backbone (for example `semantic_block_decode_size=5` with a short 10-token grouped window); these now failed cleanly on both the aligned normalized-text backbone and the mixed-text transfer branch, even while sometimes producing extremely strong in-domain WER
  - the broader exact-last grouped-step family that tries to consume all future heads with larger exact-last blocks (for example true two-head **4-token** exact/future/future/exact steps or three-head **5-token** exact/future/future/future/exact steps) by itself, including simple late-boundary schedule retunes like `4,4,1,3` and `4,4,3,1`
  - stacking the weak soft future-position-decay clue directly on top of the exact-last-token transition
  - simple transition-only exact-last schedules on just the last 1 or 2 grouped steps by themselves
  - simple early-only exact-last schedules by themselves (for example first 9 grouped tokens exact-last, then plain grouped for the final grouped step)
  - a simple periodic exact-bridge token after every grouped step by itself, including the more natural 16-token / 4-step schedule
  - simple per-head reweighting of the two grouped future heads by itself
  - extra future depth beyond the matched two-head setup, unless paired with a better transition design
  - naive early-target or early-anchor masking of the future auxiliary by itself
  - the stricter grouped-step-matched future-head supervision mask (`semantic_future_prediction_match_block_decode=true`) by itself on exact-last grouped branches, and now also on the aligned mixed-text four-head grouped mainline
  - treating soft future-position decay as a generic upgrade to the old four-head future-Q0 baseline
  - the first teacher-forced one-pass Q0 refiner prototype by itself, on either the four-head baseline or the grouped-prefix mainline
  - simple loss-weight retunes of the current realistic-input Q0 refiner prototype by themselves
  - simple probability retunes of the new future-head local-input-corruption auxiliary by themselves
  - the new per-example free-running grouped self-conditioning path by itself, including the grouped-prefix-only hybrid tail variant, the sparse 10%-mix variant, the targeted early-grouped-prefix-only replacement variant, and the newer sparse targeted early-prefix mix that falls back to the cheap one-step path on most examples; in its current form it is far too slow and already failed cleanly on the original matched grouped-prefix recipe
  - the new fully cheap targeted early-prefix self-conditioning fallback by itself on the plain grouped-prefix mainline, and simple follow-up retunes of it on the exact-last grouped branch (for example saturating `semantic_self_condition_prob` to `1.0`, stacking it with `text_loss_weight=0.1`, or stacking it with the grouped-style heavy-eval reranker)
  - the more exact-last-matched cheap targeted early-prefix variant that replaces only grouped future-token slots by itself on the plain grouped-prefix mainline, plus simple follow-up retunes/transfers of it on the exact-last grouped branch (for example saturating `semantic_self_condition_prob` to `1.0`, stacking it with the grouped-style heavy-eval reranker, or transferring it to the larger-LM branch)
  - simple schedule-aware cheap early-prefix replacement retunes on late-boundary schedules by themselves, including the earlier-taper `3,3,2,1,2`, the exact-last `3,3,3,1,2` version, whole-window replacement on `3,3,3,1,2`, and direct future-head speculative-slot replacement on `3,3,3,1,2`
  - note: this future-slots-only objective is still a live side clue when paired with the broader two-head exact/future/future/exact decoder (`semantic_block_decode_size=4`, exact-last), but it is noisy, does not survive shorter 8-token or longer 16-token grouped windows, and has not exceeded the robust `0.6667` baseline
  - simple shorter-window retunes of the cheaper piggyback future-input-corruption objective by themselves (for example shrinking the early corruption window from 12 to 6 Q0 positions)
  - cheap future-input corruption targeted only to the speculative middle-token slots of each early exact-last block by itself
  - a local exact-last middle-token lookahead by itself on 3-token grouped steps (`semantic_block_decode_compare_exact_middle_candidate=true`), where the speculative middle future token competes against the exact next-token candidate under a short 2-token exact-model lookahead; it still collapsed to `1.0` on the original grouped-prefix winner while both prompts hit the full 53-frame cap
  - a richer two-head grouped-pattern version of the cheap future-input corruption objective on the plain grouped-prefix mainline by itself
  - simple label smoothing of the auxiliary future-Q0 heads by itself (`semantic_future_prediction_label_smoothing=0.1`) on both the original grouped-prefix mainline and the broader two-head exact-last matched-training branch
  - simple stage2-gradient detachment (`detach_semantic_hidden_for_stage2=true`) on either the reproducible four-head baseline or the original grouped-prefix mainline by itself, and now also on the aligned mixed-text four-head grouped winner
  - an explicit Stage-1 audio progress / remaining-length context by itself (`semantic_audio_position_context_weight=0.25`) on both the reproducible four-head baseline and the original grouped-prefix mainline; it recovered only a weak in-domain clue on grouped-prefix while still missing the primary metric cleanly at `1.0`
  - a cheap parallel non-AR Stage-1 auxiliary by itself (`semantic_non_ar_aux_loss_weight` on top of the autoregressive decoder); it tied the robust 0.6667 baseline once at weight `0.25`, but a lower weight `0.1` and an exact rerun of `0.25` both collapsed to `1.0`, and its grouped-prefix transfer only reached weak life at `0.8333`
  - a cheap low-rank nonlinear future-Q0 head redesign by itself (replace each plain future linear head with an embed->bottleneck->CODEBOOK_SIZE MLP head); it failed on the reproducible four-head baseline, recovered only a noisy tie-level `0.6667` once on the original grouped-prefix winner, and then collapsed to `1.0` on exact rerun
  - a shared-audio-head future-prediction redesign by itself (`semantic_future_prediction_use_shared_audio_head=true`), where each future head predicts a transformed hidden state and the shared Stage-1 audio LM head maps it to Q0 logits; its first test on the aligned four-head grouped backbone regressed cleanly to `1.0`
  - a residual-audio-head future-prediction redesign by itself (`semantic_future_prediction_residual_audio_head=true`), where each future head predicts a hidden-state delta before the shared Stage-1 audio LM head maps hidden+delta to Q0 logits; its first test on the aligned four-head grouped backbone also regressed cleanly to `1.0`
  - a cheap text-anchored future-head redesign by itself (`semantic_future_prediction_text_context_weight=0.5` or `0.25`) that adds a learned prompt-text logit bias to each future-Q0 head; it recovered only weak life (`0.8333`) once on the original grouped-prefix winner with very strong CER, then collapsed to `1.0` on the lighter retune, so it already looks like another noisy grouped-side clue rather than a stable speculative-token anchor
  - a soft exact-head future-token distillation objective by itself (`semantic_future_prediction_distill_weight=0.25`) that matches each future-Q0 head to the exact next-token Q0 distribution at the corresponding future position; after the tokenizer/model-vocab alignment fix made it runnable, it still failed cleanly at `1.0` on the aligned grouped-prefix winner
  - simple post-fix transfer retunes on top of the aligned four-head **exact-decoding** baseline by themselves, including the fuller LoRA target set (`lora_targets=[q_proj,k_proj,v_proj,o_proj,gate,up,down]`) and `text_source=both`; the fuller LoRA transfer only tied at `0.8333` with weaker CER and even shorter generations, while mixed-text transfer regressed cleanly to `1.0` and lost EOS on the OOD prompt. By contrast, mixed-text conditioning is now a live clue again when paired with the aligned **grouped-prefix** decoder.
  - a cheap grouped-generation length-aware EOS bias by itself (`semantic_eos_length_bias=2.0`) on the original grouped-prefix winner; it still emitted no EOS and failed cleanly at `1.0`
  - simple post-fix EOS retunes on top of the tokenizer/model-vocab alignment branch by themselves: a blunt minimum-length floor (`semantic_gen_min_new_tokens=48`) removed EOS entirely and fell back to the full 53-frame cap at `1.0`, while lowering `semantic_eos_weight` to `1.0` kept EOS but still regressed to `1.0`
  - a cheap recent-token repetition penalty by itself during custom blockwise semantic generation (`semantic_recent_repeat_penalty=2.0`, window 8); it improved throughput but still failed cleanly at `1.0` on the original grouped-prefix winner while both prompts continued to hit the full 53-frame cap
  - a cheap text-prefix semantic-length-delta predictor by itself (`semantic_length_delta_predictor_loss_weight=0.1`) that learns how many frames to add/subtract from the current heuristic cap; it shortened generations substantially (for example to ~40/38 or 41/38 frames) but still failed on the reproducible four-head baseline and recovered only weak noisy life (`0.8333`) on the original grouped-prefix winner while badly damaging in-domain behavior
  - a cheap Stage-2 train/infer-match path that conditions the residual decoder on the Q0 stream actually fed into Stage 1 during training (`stage2_match_stage1_input_q0=true`) rather than always on ground-truth Q0 codes; it failed on the reproducible four-head baseline, recovered only weak life (`0.8333`) on the original grouped-prefix winner, and then failed badly on the exact-last grouped transfer
  - a cheap direct Stage-2 text-prefix hidden-state anchor by itself (`residual_text_context_weight=0.5`) on both the original grouped-prefix mainline and the reproducible four-head baseline
  - simple temporal warmup / ramp schedules for the future-Q0 auxiliary by itself (for example `semantic_future_prediction_ramp_steps=500` on the grouped-prefix mainline); one run reached only weak life at `0.8333`, but its exact rerun fell back to `1.0`
  - simple auxiliary text-prefix LM loss by itself, including the reproducible four-head baseline attempts (`0.1`, `0.05`, `0.02`), the plain grouped-prefix mainline attempt (`0.1`), the 600M two-head exact-last grouped-branch attempts (`0.1`, `0.05`), and the late-boundary grouped-schedule combo with `text_loss_weight=0.1`
  - stacking the weak text-prefix LM anchor with the grouped-style heavy-eval reranker on the 600M two-head exact-last grouped branch
  - simple larger-base-LM swaps by themselves (for example `QWEN3_1_7B`) on the plain four-head baseline, the original grouped-prefix mainline, or the old frozen non-AR Stage-1 branch within the same 10-minute budget
  - trying to transfer the grouped-prefix idea directly to the old four-head recipe without a stronger train/infer match, including the newer exact-last-token grouped transition
  - dropping `text_source=both` for the grouped-prefix mainline
  - explicit normalized/raw text-variant marker tokens on the aligned mixed-text grouped branch (`text_source_markers=true` with learned `<|text_normalized|>` / `<|text_raw|>` tokens); both obvious eval-marker choices failed to stabilize the branch, reaching only `0.8333` with the raw marker and `1.0` with the normalized marker
  - the first balanced one-variant-per-audio mixed-text formulation (`text_source=both_balanced`, alternating normalized/raw by utterance index) on the aligned grouped branch; it failed at `1.0` both on the plain mainline and when paired with the reproducible fuller-LoRA fallback
  - the raw-heavy weighted mixed-text formulation (`text_source=both_plus_raw`, oversample raw 2:1 over normalized) on the aligned grouped mainline; it failed cleanly at `1.0`, so the live weighted direction is not toward more raw text
  - stacking the newer normalized-heavy weighted mixed-text clue with the reproducible fuller-LoRA fallback; the old hardcoded 2:1 mix only reached `0.8333`, the newer generic 3:1 weighted mix collapsed all the way to `1.0`, and the later **2-pass sampled 3:1** grouped branch also failed cleanly at `1.0`, so fuller LoRA is not a stabilizer for this weighted family either
  - pushing the generic weighted-text branch too far toward normalized text by itself (`text_source=both_weighted` with repeats 4/1); it first returned `0.6667` but then collapsed to `1.0` on exact rerun, so the extra gain does not survive at that more extreme normalized-heavy ratio
  - the milder generic weighted-text ratio closer to the original 1:1 mix (`text_source=both_weighted` with repeats 3/2); it only reached `0.8333`, so the useful weighted region does not look centered right next to the original 1:1 branch either
  - the seemingly intermediate generic weighted-text ratio (`text_source=both_weighted` with repeats 5/2); despite perfect in-domain WER it regressed cleanly to `1.0`, so this family is not monotonic inside the nominal 2:1–3:1 sweet spot
  - the first duplication-free balanced weighted-text formulation (`text_source=both_weighted_balanced` with repeats 3/1); it failed cleanly at `1.0`, so the live weighted clue does not come from simply choosing one weighted text variant per utterance instead of duplicating examples
  - the non-live neighbors of the new sampled stochastic-mass branch: the full-mass per-pass weighted one-variant resampling formulation (`text_source=both_weighted_sampled` with repeats 3/1 and the default **4** sampled passes) failed cleanly at `1.0`, the nearby lower-mass **1-pass** and **3-pass** variants also failed cleanly at `1.0`, even inside the live **2-pass** sampled family the nearby **2:1** and **4:1** normalized/raw ratio probes both failed cleanly at `1.0`, multiple alternate deterministic sampled-assignment seeds also caused catastrophic `1.0` OOD failures, a coverage-aware **without-replacement** reformulation still only reached `0.8333`, and a low-mass **one normalized pass + one balanced pass** reformulation also failed cleanly at `1.0`. So the sampled weighted branch currently looks tightly localized around the new **2-pass, 3:1, seed-0** setting rather than like a broad sampled-family upgrade.
  - the generic 2:1 weighted transfer onto the aligned four-head exact-decoding backbone (`text_source=both_weighted` with repeats 2/1 and no grouped prefix); it failed cleanly at `1.0`, so the one exact-decoding weighted success does not extend to normalized-heavy weighting in general
  - the more normalized-heavy 4:1 weighted transfer onto that same exact-decoding backbone; it also failed cleanly at `1.0`, so the one exact-decoding weighted success does not extend monotonically toward higher normalized-text mass either
  - treating weighted-text exact-decoding transfers as a new mainline: the older fixed-duplicate 3:1 transfer onto the aligned exact-decoding backbone hit `0.6667` once and then collapsed twice to `1.0`, while the newer 2-pass sampled 3:1 branch also failed cleanly when transferred to exact decoding. So weighted exact-decoding currently looks mostly stale/noisy rather than reproducible, and the new sampled clue appears grouped-prefix-specific.
  - grouped-prefix length retunes on the live grouped 3:1 weighted branch away from the original 12-token window: shrinking the grouped window to **6** tokens only reached `0.8333` while cutting throughput sharply, while **9** and a single **3**-token grouped step both failed cleanly at `1.0`, and extending it to **15** also failed cleanly at `1.0`, so this branch now looks tightly localized around the original 12-token grouped prefix
  - the historical exact-last grouped transition on that same weighted 3:1 branch (`semantic_block_decode_exact_last_token=true` with the 12-token grouped window); it also regressed cleanly to `1.0`, so exact-last does not rescue the weighted path either
  - the short late-bridge schedule `semantic_block_decode_schedule=3,1,2` on that same weighted 3:1 grouped branch; it also regressed cleanly to `1.0`, so the late-boundary bridge+taper family does not rescue the weighted branch even when shortened to a 6-token schedule
  - simple Stage-1 upweight / Stage-2 downweight on the live weighted 3:1 grouped branch (`semantic_loss_weight=2.0`, `acoustic_loss_weight=0.5`); it also regressed cleanly to `1.0` while cutting sample coverage to only ~18.1k / 10 min, so this older semantic-emphasis retune is stale for the weighted branch too
  - simple text-prefix LM anchors on that same weighted 3:1 grouped branch (`text_loss_weight=0.1` and `0.05`); the first `0.1` run only tied the usual `0.8333` level with heavy throughput loss, its exact rerun collapsed to `1.0`, and the milder `0.05` retune failed even harder at `1.0`, so this text-loss family is stale/noisy for the weighted branch rather than a stabilizer
  - the late-boundary schedule clue on top of the live 3:1 weighted-text branch (`semantic_block_decode_schedule=3,3,3,1,2`); it also regressed cleanly to `1.0`, so this older schedule family does not stabilize the weighted branch either
  - using the smaller one-head / two-token matched grouped branch as the main grouped-prefix path
  - matching the exact-last-token transition by shrinking training to a single future-Q0 head by itself
  - using larger four-token early grouped steps as the main grouped-prefix path
  - mild residual-robustness noise (`q0_corruption_prob`, `residual_semantic_hidden_dropout_prob`) on top of the strong structural recipes
  - simply shrinking or trimming LoRA adaptation on the reproducible four-head baseline by itself (for example `lora_rank=lora_alpha=8`, attention-only `[q_proj,v_proj]`, or attention-only `[q_proj,k_proj,v_proj,o_proj]`)
  - simple local retunes around the new fuller LoRA target-set clue by themselves, including mixed-text stacking, smaller rank/alpha (`8/8`), lower alpha with full rank (`alpha=8`), stronger alpha (`32`), more surgical attention+down or MLP-only target subsets, transferring it directly to either grouped-prefix branch (including the two-head exact-last variant), transferring it to the larger `QWEN3_1_7B` plain baseline, stacking it with a simple text-prefix LM anchor, or stacking it with the grouped-style heavy-eval reranker on the two-head exact-last grouped branch
  - simple stage-1 loss upweighting / stage-2 loss downweighting by itself on the grouped-prefix branches (for example `semantic_loss_weight=2.0 acoustic_loss_weight=0.5`), plus the newer linear Stage-2 acoustic-loss warmup / delay path (`acoustic_loss_ramp_steps`) on both the reproducible four-head baseline and the grouped-prefix winner; the long 1000-step ramp failed cleanly on the robust baseline, a 500-step ramp on the grouped branch only recovered weak secondary signals, a shorter 250-step ramp became an in-domain-only side effect while exploding OOD CER, and the same 2.0/0.5 retune also failed cleanly on the aligned mixed-text fuller-LoRA fallback with OOD EOS disappearing again
  - simply shortening optimizer warmup on the reproducible four-head baseline (for example `warmup_steps=25`) by itself
  - simple global batch-size scaling on the reproducible four-head baseline by itself (now tried both smaller `batch_size=4` and larger `batch_size=12`), plus a smaller-batch transfer onto the aligned mixed-text four-head grouped winner; that raised update count but still regressed cleanly to `1.0`, so the branch is not hiding a simple step-limited win
  - simply shrinking Stage 2 by itself on either the reproducible four-head baseline or the original grouped-prefix mainline (for example residual 2 layers / 2 heads / MLP 256)
  - LoRA dropout on the current LLM training path without code changes; it currently crashes because the LoRA layers do not receive a PRNG key during forward passes. A first full keyed implementation was also tried and then reverted because it was far too expensive and harmful even at `lora_dropout=0.0`, so this idea is stale unless a much cheaper keyed path is found.
- If this grouped-prefix branch keeps looking too noisy, the fallback pivot is still a **stronger semi-parallel / hierarchical Stage-1 decoder** rather than the old lightweight non-AR interpolator. Two concrete Stage-1.5 refiner pivots have now been tried: (1) a teacher-forced one-pass Q0 refiner and (2) a better-matched refiner trained on the actual Q0 stream seen by Stage 1, including self-conditioned suffixes. Both failed on the primary metric on both the reproducible four-head baseline and the grouped-prefix mainline, though the realistic-input version recovered some secondary signal before regressing again under a simple weight retune. So Stage-1.5 refinement is not dead, but the current lightweight one-pass prototypes are now low priority unless they are redesigned around truly free-running/generated Q0 error distributions. For now the best new evidence still concentrates on the early-grouped-prefix autoregressive path, even though that branch is heavily pruned.
ibutions. For now the best new evidence still concentrates on the early-grouped-prefix autoregressive path, even though that branch is heavily pruned.
trates on the early-grouped-prefix autoregressive path, even though that branch is heavily pruned.
